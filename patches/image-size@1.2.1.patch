diff --git a/dist/types/icns.js b/dist/types/icns.js
index f2bfafef3723cb423b110304815e56e3f97f81e0..dbe256aefc6c304764339ba74c524f4e6e3a3c74 100644
--- a/dist/types/icns.js
+++ b/dist/types/icns.js
@@ -65,6 +65,14 @@ const ICON_TYPE_SIZE = {
 };
 function readImageHeader(input, imageOffset) {
     const imageLengthOffset = imageOffset + ENTRY_LENGTH_OFFSET;
+    if (imageOffset < SIZE_HEADER || imageOffset + 8 > input.length) {
+        throw new TypeError('Invalid ICNS entry header');
+    }
+    const length = (0, utils_1.readUInt32BE)(input, imageLengthOffset);
+    const fileLength = (0, utils_1.readUInt32BE)(input, FILE_LENGTH_OFFSET);
+    if (fileLength > input.length || length < 8 || imageOffset + length > fileLength) {
+        throw new TypeError('Invalid ICNS entry length');
+    }
     return [
         (0, utils_1.toUTF8String)(input, imageOffset, imageLengthOffset),
         (0, utils_1.readUInt32BE)(input, imageLengthOffset),
diff --git a/dist/types/utils.js b/dist/types/utils.js
index 5224bbafe87551ac415cb3de234820ccc0ff6e2c..5fbdf610b341db862d106ca0a80185206a67c9aa 100644
--- a/dist/types/utils.js
+++ b/dist/types/utils.js
@@ -65,6 +65,9 @@ function findBox(input, boxName, offset) {
         const box = readBox(input, offset);
         if (!box)
             break;
+        if (box.size < 8 || !Number.isSafeInteger(box.size) || offset + box.size > input.length) {
+            throw new TypeError('Invalid image box length');
+        }
         if (box.name === boxName)
             return box;
         // Fix the infinite loop by ensuring offset always increases
