/**
 * 약관·개인정보처리방침·고객센터가 함께 쓰는 연락처·시행일.
 * 세 문서에 흩어져 있으면 한쪽만 고쳐 서로 다른 주소를 안내하게 된다.
 *
 * ⚠️ 도메인(tripick.place)은 확정됐고 발신(noreply@)은 Resend 로 동작하지만,
 * 아래 두 주소는 **수신** 라우팅이라 별도 설정이 필요하다.
 */
export const SUPPORT_EMAIL = 'support@tripick.place';
export const PRIVACY_EMAIL = 'privacy@tripick.place';

/** 약관·개인정보처리방침 공통 시행일. 내용을 고치면 여기도 함께 올린다. */
export const LEGAL_UPDATED_AT = '2026년 8월 28일';
