# TriPick 전국 적재 후 랭킹 재튜닝 v1

문서 목적: 적재 커버리지를 74% → 97% 로 채우고 **그 상태에서 랭킹 상수를 다시 잡는다.** 결과는 둘로 갈렸다 — 후보 풀 배수는 카탈로그 크기에 묶여 있어 반드시 재조정이 필요했고(R@10 +0.067), 나머지 네 노브는 4배 커진 카탈로그에서도 기존 값이 그대로 최적이었다. 그리고 **커버리지를 채우면 `R|cat` 이 떨어진다** — 그게 정상이라는 것을 고정한다.

작업 브랜치: `feat/destination-anchor-retrieval`
작성일: 2026-08-18
관련 문서: [`retrieval-eval-harness-hardening-v1.md`](./retrieval-eval-harness-hardening-v1.md) (측정 방법론·골든셋 교정), [`crag-term-weight-tuning-v1.md`](./crag-term-weight-tuning-v1.md) (항목 가중 구조·AUC 방법), [`catalog-ingestion-gates-v1.md`](./catalog-ingestion-gates-v1.md) (적재 게이트), [`popular-place-ingestion-v1.md`](./popular-place-ingestion-v1.md)

---

## 1. 전국 적재

```bash
KTO_FETCH_OPENING_HOURS=false pnpm ingest:places -- --sources=tour --max=10000
```

| | 전 | 후 |
| --- | --- | --- |
| 카탈로그 | 10,721행 | **40,304행** |
| KTO 소스 | 4,879 | 34,462 |
| 축제 기간 보유 | 71 | **909** (그중 종료 705 = 78%) |
| 테이블 크기 | — | 565MB |

수집 35,421 → 신규 29,583 / 갱신 3,636 / 유지 1,195 / 중복 852. KTO 전국 48,857건 중 약 13,400건이 게이트(숙박·쇼핑 소매 점포·부적합 장소·좌표 불량)에서 걸렸다. **예산 초과 없이 한 번에 끝났다** — 목록 ~505콜 + 축제 기간 ~25콜.

영업시간을 켜면 건당 1콜이라 48,857콜(예산 900/일 기준 54일)이 되므로 반드시 끈다. 대신 `arrange='O'`(대표이미지 있는 순)라 실측 서울 8,000위까지 이미지 100% 로, 전량을 받아도 이미지 없는 롱테일이 거의 안 딸려온다.

## 2. ⚠️ 커버리지를 채우면 `R|cat` 이 떨어진다 — 그게 정상이다

| | 적재 전 | 적재 후(배수 10) |
| --- | --- | --- |
| 적재 커버리지 | 74% | **97%** |
| R@10 | 0.372 | 0.356 |
| **R\|cat** | **0.634** | **0.426** |
| MRR | 0.823 | 0.690 |
| pgvector 단독 | 14/16 | **16/16** |

`R|cat` 은 "**카탈로그에 있는** 정답 중 상위에 든 비율"이라 분모가 커버리지에 묶여 있다. 전에는 정답의 74% 만 분모였고 그중 잘 찾았다는 뜻이고, 지금은 97% 가 분모다. **이전 0.634 와 이후 0.426 은 다른 모집단의 값이라 직접 비교할 수 없다.**

커버리지가 이미 100% 였던 케이스를 보면 순수 랭킹 열화가 분리된다 — 강릉 0.60→0.30, 경주 0.80→0.40, 속초 0.75→0.40. 후보 풀이 3.8배가 되어 경쟁이 세진 결과이고, 이게 §3 의 출발점이다.

카카오 폴백이 **완전히 사라진 것**(14/16 → 16/16)은 카탈로그가 자립했다는 신호다.

## 3. 후보 풀 배수 — 카탈로그 크기에 묶인 값

풀은 순전히 벡터 거리로 선발되고 인지도·취향은 그 뒤 재정렬만 한다. **풀에 못 들어온 장소는 인지도가 아무리 높아도 결과에 나올 길이 없다.**

배수 10 은 카탈로그 10,333행 시절의 무릎이었다 — 경기 615행 중 풀 160 = **26%**. 전국 적재로 경기가 6,524행이 되자 같은 풀이 **2.5%** 가 됐다.

| 배수 | 풀 | R@10 | R\|cat | 응답(경기) |
| --- | --- | --- | --- | --- |
| 10 | 160 | 0.356 | 0.426 | 53ms |
| **20** | 320 | **0.423** | 0.524 | **60ms** |
| 40 | 640 | 0.417 | 0.541 | 75ms |
| 80 | 1280 | 0.417 | 0.547 | 120ms |

10 → 20 이 **R@10 +0.067 · R\|cat +0.098 을 7ms 로** 얻는다. 40 은 R\|cat 을 0.017 더 주지만 R@10 이 내려가고, 80 은 비용이 2.3배인데 이득이 거의 없다.

**SQL 은 LIMIT 과 거의 무관하다**(경기 기준 33~35ms 고정) — 지역 pre-filter 가 이미 그 지역 전체 행의 거리를 계산하므로 LIMIT 은 top-N 힙 크기만 바꾼다. 늘어나는 건 Node 재정렬이고, 특히 `collapseNearDuplicates` 가 O(n²) 라 풀이 커질수록 이쪽이 지배한다(53 → 120ms 의 정체).

## 4. 나머지 네 노브는 기존 값이 그대로 최적이었다

전부 `--sweep` 으로 한 프로세스 안에서 쟀다([`retrieval-eval-harness-hardening-v1.md` §3](./retrieval-eval-harness-hardening-v1.md)).

| 노브 | 값 | 스윕 결과 |
| --- | --- | --- |
| `CRAG_RETRIEVAL_WEIGHT` | 0.06 유지 | 0~0.06 이 R@10 0.423 으로 평평, 0.12 부터 0.410·0.393 으로 하락 |
| `PREFERENCE_BLEND_WEIGHT` | 0.85 유지 | 0.85 가 R\|cat 0.524 최고 (0.75→0.517, 0.95→0.511, 1→0.511) |
| `CRAG_MIN_POPULARITY_COVERAGE` | 0.05 유지 | 0.02~0.05 동률 최적, 0.10 부터 하락, **0.20 은 R\|cat 0.301 로 급락** |
| `CRAG_ANCHOR_EDGE_SCORE` | 0.7 유지 | 0.7 이하 R@10 0.510 로 평평(0.95·0.85 는 0.427). 후보가 늘어 이전의 0.6 이하 손해가 사라졌지만 0.7 에서 이미 최적 |

이전 튜닝이 카탈로그 4배 변화에도 견뎠다는 뜻이다. **재조정이 필요한 노브는 "카탈로그 크기에 비례하는 양"을 정하는 것뿐이다**(풀 배수). 점수 항의 상대 비율은 카탈로그 크기와 무관했다.

## 5. 항목별 AUC 재측정 (풀 2,127건)

[`measure-retrieval-similarity.ts`](../../apps/api/src/scripts/measure-retrieval-similarity.ts) 로 다시 쟀다. 가중을 건드리기 전에 신호 품질의 서열을 확인하는 것이 이 저장소의 순서다.

| term | 가중 | AUC(케이스평균) | p5 | p50 | p95 | 실효 스프레드 |
| --- | --- | --- | --- | --- | --- | --- |
| **popularity** | 0.16 | **0.736** | 0.150 | 0.150 | 0.980 | **0.134** |
| taste | 0.27 | 0.646 | 0.540 | 0.651 | 0.778 | 0.064 |
| retrieval | 0.06 | 0.599 | 0.718 | 0.765 | 0.796 | 0.005 |
| personalization | (내부) | 0.578 | 0.655 | 0.703 | 0.745 | — |
| locality | 0.21 | **0.518** | 0.888 | 0.920 | 0.920 | **0.007** |
| context | 0.17 | **0.500** | 0.633 | 0.633 | 0.802 | 0.030 |
| availability | 0.12 | **0.500** | 0.580 | 0.580 | 0.580 | 0.000 |

`naver-unmentioned` 발동률 75.9% — 인지도 항은 활발히 일한다.

읽히는 것 둘:

1. **popularity 가 여전히 최고 신호**(AUC 0.736, 실효 스프레드 0.134)인데 가중은 taste(0.27)의 **0.6배**다. taste 는 AUC 0.646 에 스프레드 0.064 로 절반이다.
2. **`locality` 가중 0.21 이 거의 상수인 항에 묶여 있다**(p5 0.888 · p50 0.920 · p95 0.920, 스프레드 0.007). 지역 pre-filter 가 이미 후보를 좁혀 전 후보가 일치하기 때문이다 — 앵커 케이스에서만 거리로 변한다. `context`(0.17)·`availability`(0.12)도 골든셋에서는 상수다.

## 6. 하지 않은 것 — popularity : taste 비율

§5 는 "popularity 가중을 올리고 taste 를 내리면 좋아진다"를 가리킨다. **이번에는 손대지 않았다.**

이유는 [`crag-term-weight-tuning-v1.md`](./crag-term-weight-tuning-v1.md) 에 이미 기록된 구조적 함정이다 — **골든셋 정답이 '그 목적지의 유명 명소'라 popularity 를 올리면 지표가 자동으로 좋아진다.** 그 개선이 실제 품질인지 지표 과적합인지 이 골든셋으로는 가를 수 없다. `personalization` AUC 0.578 이 낮은 것도 같은 이유로 개인화를 깎는 근거가 못 된다(정답이 개인 취향과 무관하게 뽑혔다).

`locality`·`context`·`availability` 의 죽은 가중을 옮기는 안도 같은 문서에 **전 구간 무효**로 기록돼 있다. 가중 합이 1 로 정규화되므로, 상수 항의 몫을 일하는 항들에 **비례 배분**하면 그들 사이의 상대 비율이 유지되어 순위가 바뀌지 않는다. 순위를 바꾸려면 popularity : taste 비율 자체를 건드려야 하고, 그게 곧 위 함정이다.

이 항목을 열려면 골든셋 밖의 신호가 필요하다 — 개인화 품질을 측정하는 별도 셋(같은 목적지·다른 취향 벡터로 결과가 갈리는지), 또는 실사용 로그.

## 7. 새 기준선 (앞으로의 비교 기준)

전국 적재 + 풀 배수 20 확정 상태. **커버리지가 74% → 97% 로 바뀌었으므로 이 이전 지표와는 비교하지 말 것**(§2).

```
R@5 0.228 | R@10 0.423 | R|cat 0.524 | MRR 0.690
적재 커버리지 97% | 지역정합 100% | 금지어 0 | conf 0.749 | pgvector 단독 16/16
```

케이스별(`R|cat` 오름차순 — 위쪽이 아픈 곳):

| case | R@5 | R@10 | R\|cat | catalog | MRR | 놓친 정답 | 병목 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| seoul-city-nightview | 0.10 | 0.20 | **0.20** | 100% | 0.33 | 8 | 랭킹 |
| daegu-nostalgic | 0.08 | 0.23 | **0.23** | 100% | 0.20 | 10 | 랭킹 |
| sokcho-mountain | 0.10 | 0.20 | **0.40** | 100% | 0.33 | 6 | 랭킹 |
| busan-beach | 0.23 | 0.38 | **0.46** | 100% | 0.50 | 7 | 랭킹 |
| jeju-nature | 0.30 | 0.50 | **0.50** | 100% | 1.00 | 5 | — |
| gangneung-cafe-beach | 0.20 | 0.40 | **0.50** | 100% | 1.00 | 5 | — |
| yeosu-romantic-island | 0.30 | 0.40 | **0.50** | 100% | 1.00 | 5 | — |
| gyeongju-weather-indoor | 0.14 | 0.29 | **0.50** | 86% | 0.50 | 3 | 커버리지 |
| taebaek-deviation-anchored | 0.17 | 0.33 | **0.50** | 100% | 0.33 | 3 | — |
| seomyeon-station-anchor | 0.23 | 0.38 | **0.55** | 85% | 1.00 | 5 | 커버리지 |
| jeonju-food-hanok | 0.22 | 0.44 | **0.57** | 78% | 1.00 | 3 | 커버리지 |
| gwangju-culture | 0.30 | 0.40 | **0.60** | 100% | 0.50 | 4 | — |
| sunchang-late-evening | 0.20 | 0.60 | **0.60** | 100% | 0.33 | 2 | — |
| gwangalli-anchor | 0.27 | 0.64 | **0.64** | 100% | 1.00 | 4 | — |
| gyeongju-cultural | 0.30 | 0.70 | **0.80** | 100% | 1.00 | 2 | — |
| yeongju-thin-catalog | 0.50 | 0.67 | **0.83** | 100% | 1.00 | 1 | — |

재현성 — 같은 커밋으로 10분 간격 두 번 돌려 `R@10`·`R|cat`·`MRR`·커버리지가 소수점까지 일치했고 `R@5` 만 0.221↔0.228 로 갈렸다(광주 한 케이스). 그게 인지도 코퍼스 변동 폭이므로, **`R@5` 의 ±0.01 은 신호로 읽지 말 것.**

## 8. 랭킹 실패 네 케이스 진단과 처방

기준선(§7)에서 커버리지 100% 인데 `R|cat` 이 낮은 넷을 파고들었다. **네 케이스가 서로 다른 이유로 실패한다.**

### 8.1 먼저 갈라야 할 것 — 선발이냐 재정렬이냐

원시 풀(`searchByEmbedding` 반환)과 채점 풀(`rank` 반환)을 동시에 가로채 정답의 위치를 추적했다.

| case | 선발 실패(풀에 없음) | 재정렬 실패(밀림) | 선택 |
| --- | --- | --- | --- |
| seoul-city-nightview | **6** | 2 | 2 |
| daegu-nostalgic | **5** | 5 | 3 |
| busan-beach | 3 | 4 | 6 |
| sokcho-mountain | 0 | 6 | 4 |

**선발 실패는 점수로 못 고친다** — 풀은 순전히 벡터 거리로 뽑힌다.

### 8.2 근접 중복 접기가 정답을 삼켰다 (버그, 수정함)

`속초해수욕장` 이 원시 풀 선발 3위였는데 **채점 풀에 아예 없었다.** 긴 이름 하나가 서로 무관한 짧은 이름 둘을 이어 붙이는 다리 병합이었다 — 상세는 [`near-duplicate.ts`](../../apps/api/src/planner/retrieval/near-duplicate.ts) 주석. 접기를 한 방향으로 바꿔 고쳤다: sokcho `R|cat` 0.40→0.50.

### 8.3 taste 가 인지도를 이긴다 (게이트로 완화)

```
sokcho 신흥사   pop 1.00  tst 0.54  → 17위      sokcho 속초항아리물회  pop 0.96  tst 0.81  → 1위
sokcho 영금정   pop 1.00  tst 0.54  → 19위      busan  오시리아 해안산책로 pop 0.92 tst 0.82 → 1위
busan  오륙도   pop 1.00  tst 0.56  → 19위
```

`taste` 0.28 차이 × 가중 0.27 = 0.076 vs `popularity` 0.08 차이 × 가중 0.16 = 0.013 — **taste 가 6배 강하다.** 그리고 taste 가 낮은 이유는 취향 불일치가 아니라 **태그 사전이 이름에서 아무것도 못 읽은 것**이다(신흥사·영금정·오륙도 모두 `cultural` 하나).

`CRAG_TASTE_FALLBACK_NEUTRAL` 로 폴백 태그뿐인 후보를 중립으로 두자 부산 0.46→0.54, 속초 0.50→0.60 으로 올랐다. **커버리지 74% 시절 같은 변경은 개선 5/악화 5 로 갈려 버렸던 것**이라(§[harness §4.2](./retrieval-eval-harness-hardening-v1.md)) 커버리지가 전제였다.

### 8.4 ⚠️ 선발 실패의 원인은 태그가 임베딩 텍스트에 들어간다는 것

풀 배수를 60(서울 4,110행의 23%)까지 올려도 **seoul `R|cat` 0.20 이 꿈쩍도 안 했다.** 정답 6개의 태그를 찍어 보면 이유가 나온다:

```
질의 취향: cafe, trendy, city, nightview

청계천            → cultural          명동거리        → cultural
북촌한옥마을       → village, cultural  홍대걷고싶은거리 → cultural
롯데월드타워       → cultural          동대문디자인플라자 → cultural
```

`buildPlaceEmbeddingText` 가 `name | category | address | tags` 라 **태그가 벡터에 그대로 들어간다.** 태그가 `cultural` 하나면 도심·야경 질의와 벡터 거리가 멀어 **선발 단계에서 이미 탈락**한다. `'거리'`·`'타워'`·`'천'` 이 사전에 없는 것이 직접 원인이다.

즉 taste 게이트는 **점수만** 고친다. 선발까지 고치려면 태그 사전을 늘리고 **재적재(재임베딩)** 해야 한다 — 자연 지형 접미(봉·바위·굴)를 넣을 때와 같은 방식으로 카탈로그 실측 오탐률을 재서 추가하고, `text_hash` 가 바뀐 행만 재임베딩된다.

### 8.5 풀 배수는 20 이 여전히 옳다

재정렬을 고친 뒤 다시 스윕해도 20 이 최적이다(40·60 은 `R|cat` +0.012 인데 R@10 0.431→0.419, R@5 0.226→0.214). 배수를 올려 새로 들어오는 정답은 상위 10 에 못 들고 다른 정답을 밀어낸다.

### 8.6 이 라운드 결과

| | 기준선 | 접기 수정 | + taste 게이트 |
| --- | --- | --- | --- |
| R@5 | 0.234 | 0.234 | 0.226 |
| R@10 | 0.429 | 0.429 | **0.431** |
| R\|cat | 0.530 | 0.530 | **0.537** |
| MRR | 0.700 | 0.700 | **0.723** |

(접기 수정 전 기준선은 0.228/0.423/0.524/0.690. R@5 의 ±0.01 은 노이즈이므로 신호로 읽지 않는다 — §7)

## 9. 태그 사전 확장 — 선발 실패를 고친다

§8.4 의 진단에 따라 도심 접미를 넣었다. 태그는 임베딩 텍스트에도 들어가므로 이 사전을 고치면 **점수뿐 아니라 선발이 바뀐다** — 그게 풀 배수로는 안 풀리던 부분이다.

카탈로그 실측(40,304행)으로 오탐률을 재서 골랐다:

| 접미 | 총 | 관광지 | 태그 | 판정 |
| --- | --- | --- | --- | --- |
| 거리 | 252 | 247 (98%) | `city` | 채택 — 인사동거리·청담패션거리·봉산문화거리 |
| 광장 | 78 | 78 (100%) | `city` | 채택 — BIFF광장·송상현광장·서울광장 |
| 타워 | 30 | 29 | `nightview` `city` | 채택 — 남산서울타워·부산타워·83타워 |
| 상점가 | 18 | 18 (100%) | `city` | 채택 |
| **천** | — | — | — | **제외** |

가드 둘 — `삼거리`·`사거리`·`오거리`·`육거리` 는 **교차로**라 뺐고(실측 오탐 '전포 삼거리'), 뒤에 한글이 이어지면 제외했다('광장동'=서울 광진구 지명, '타워팰리스'=아파트).

⚠️ `'천'`(川)은 넣지 않았다. 청계천을 잡으려면 필요하지만 **시·군 이름이 '천'으로 끝나는 곳이 너무 많다** — 이천·포천·춘천·인천·순천·제천·연천·과천·김천·사천·합천·옥천·영천·예천·진천·홍천·화천. `…산` 에서 `REGION_NAMES_ENDING_IN_SAN` 으로 데인 것과 같은 함정이다.

### 9.1 재적재로 임베딩을 갱신해야 한다

태그는 점수(`inferPlaceTags`, 런타임 계산)와 임베딩 텍스트(저장된 벡터) 양쪽에 들어간다. 선발까지 고치려면 후자를 다시 만들어야 한다. 재적재에서 `text_hash` 가 바뀐 **335행만** 재임베딩됐다(신규 0 / 갱신 335 / 유지 34,079).

⚠️ **DB 만 읽어 재임베딩할 수는 없다.** `buildText` 가 `categoryDetail` 을 쓰는데 `place_embeddings` 에 그 컬럼이 없어, DB 에서 텍스트를 재구성하면 적재 경로와 해시가 어긋나 다음 적재가 전량을 다시 재임베딩한다. `category_detail` 컬럼을 추가하면 이게 풀리고 §3.3 의 의료 오탈락도 함께 풀린다.

### 9.2 결과 — 한 케이스만 변했고 순수 개선

| case | R@10 | R\|cat | MRR |
| --- | --- | --- | --- |
| seoul-city-nightview | 0.20 → **0.30** | 0.20 → **0.30** | 0.20 → **1.00** |

`hits`: 성수동카페거리·서울숲 → **롯데월드타워** + 성수동카페거리·서울숲. `'타워'`(nightview·city)가 롯데월드타워를 선발에 진입시키고 **1위까지** 올렸다. 나머지 15케이스는 완전히 동일하다.

전체: R@5 0.226→0.232 · R@10 0.431→**0.437** · R\|cat 0.537→**0.543** · MRR 0.723→**0.773**.

`'거리'` 를 받은 명동거리·홍대걷고싶은거리는 **아직 못 들어온다** — `city` 하나로는 야경 질의(cafe·trendy·city·nightview) 네 태그 중 하나만 맞아 부족하다. 청계천·동대문디자인플라자·북촌한옥마을은 §9 표의 이유로 이번 라운드 대상이 아니다.

## 10. `category_detail` 컬럼 — 태그 반복 수정을 싸게 만든다

§9.1 의 제약을 없앤다. 마이그레이션 [`1786500000000-AddPlaceCategoryDetail`](../../apps/api/src/database/migrations/1786500000000-AddPlaceCategoryDetail.ts) 이 소스 카테고리 상세를 저장한다. 세 가지가 이 한 컬럼에 묶여 있었다.

1. **DB 만 읽어 재임베딩할 수 없었다** — 임베딩 텍스트에 `categoryDetail` 이 들어가는데 저장을 안 해서, 태그 사전을 한 줄 고칠 때마다 KTO 전량 재적재(회당 ~505콜·15분)가 필요했다
2. **적재 게이트와 검색 게이트가 같은 입력을 못 봤다** — 검색 후보에 categoryDetail 이 없어 적재에서도 **일부러 빼고** 판정했다
3. **이름 기반 의료 필터의 오탈락**을 가를 근거가 없었다

### 10.1 의료 오탈락이 풀렸다

`TRAVEL_CATEGORY_KEYWORDS` 에서 `'쇼핑'` 을 뺐다 — KTO 쇼핑(38)에는 전통시장(여행지)과 약국(여행지 아님)이 함께 있어 "소스가 쇼핑으로 줬다"가 여행 대상임을 보증하지 못한다. 반대로 `'관광'` 은 그대로 두면 관광지(12)로 분류된 실제 명소를 살린다.

| 후보 | 소스 카테고리 | 전 | 후 |
| --- | --- | --- | --- |
| 가까운약국 · 서면365약국 | 쇼핑 | 제외 | 제외 |
| 경주 중앙시장 | 쇼핑 | 통과 | 통과 |
| **부산 구 백제병원**(등록문화재) | 관광지 | **제외** | **통과** |
| **충북대학교병원 치유의나눔길** | 관광지 | **제외** | **통과** |
| 더바디성형외과의원 | 관광지 | 제외 | 통과 (KTO 가 관광지로 분류한 1건, 감수) |

### 10.2 [`reembed:places`](../../apps/api/src/scripts/reembed-places.ts)

```bash
pnpm reembed:places                    # dry-run
pnpm reembed:places -- --apply --regions=서울,부산
```

적재의 `buildText` 와 **글자까지 같은** 텍스트를 DB 에서 만들고, 해시가 바뀐 행만 재임베딩한다. 한쪽만 고치면 두 경로가 서로의 행을 영영 재임베딩하므로 이 동일성이 전제다 — 백필된 407행에서 **해시 불일치 0건**으로 검증했다.

⚠️ 안전장치 — `category_detail` 이 NULL 인 행은 **건너뛴다.** 그 행의 저장된 텍스트는 적재 당시의 categoryDetail 을 포함해 만들어졌는데 그 값을 모르므로, 여기서 detail 없이 재구성하면 더 빈약한 텍스트로 덮어쓰고 다음 적재가 또 재임베딩한다.

### 10.3 ⚠️ HTTP 429 가 "적재 완료"로 보고됐다 (수정함)

백필을 돌렸더니 서울 8페이지째부터 전부 429 였는데, 지역마다 실패 경고만 찍고 16개 시도를 끝까지 돌고 **"적재 완료: 신규 0 / 갱신 0"** 으로 끝났다. **"할 일이 없었다"와 "전부 실패했다"가 같은 보고**로 나온 것이다. 행 수를 직접 세지 않으면 성공으로 오독한다.

원인 — `detectKtoQuota` 는 KTO 가 **HTTP 200 본문**에 실어 보내는 초과 메시지를 보는데, 429 는 axios 가 던져 일반 catch 로 빠진다. 429 를 초과로 취급해 예산을 소진 처리하고 상위로 던지도록 고쳤다.

### 10.4 현재 상태 — 백필 미완

전량 적재를 하루에 세 번 돌려 KTO 일일 한도를 소진했다. `category_detail` 은 **407 / 40,304행**만 채워졌다. 컬럼이 nullable 이라 동작은 깨지지 않지만(NULL → 지금과 동일), 이 과도기에는 일부 행만 categoryDetail 을 가져 태그 판정이 일관되지 않는다 — 지표가 미세하게 내렸다(R@10 0.437→0.430, R\|cat 0.543→0.537, 노이즈 범위).

한도가 리셋되면 `ingest:places -- --sources=tour` 1회로 백필이 끝나고, 그 뒤부터 태그 사전 수정은 `reembed:places` 로 KTO 없이 몇 분에 돈다.

## 11. 남은 것

- `daegu-nostalgic` R\|cat 0.23, `seoul-city-nightview` 0.20 — 커버리지 100% 인데 낮다. 순수 랭킹 실패가 남은 두 케이스
- `jeonju-food-hanok` 78% · `gyeongju-weather-indoor` 86% — 커버리지가 아직 안 찬 유이한 케이스. KTO 에 없는 정답(객리단길·자만벽화마을·솔거미술관)이라 카카오·popular 소스 패스가 필요하다
- 카카오 소스는 이번에 안 돌렸다(`--sources=tour` 만). 그 뒤 별도로 돌린 결과와 거기서 드러난 체인 지점 문제는 [kakao-source-enrichment-v1.md](kakao-source-enrichment-v1.md) 로 이어진다
