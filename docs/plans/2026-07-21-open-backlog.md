# TriPick 미처리 백로그 트래킹

문서 목적: 각 기능 문서(`*-v1`)의 후속/백로그 섹션을 모아, 아직 안 된 항목만 체크박스로 추적한다. CLAUDE.md 기획 범위와 대조해 계획 밖 항목은 제외했다.

작성일: 2026-07-21
기준선: 최신 문서 `day-scoped-replan-v1`·`deployment-railway-vercel-runpod`(2026-07-27)까지. `[코드확인]` = 미처리 여부를 코드로 대조한 항목.
관련 문서: [docs/README.md](../README.md) (문서 인덱스), [CLAUDE.md](../../CLAUDE.md) (기획 범위·non-goal)

## 상태 표기

체크박스는 "완료/미완료"만 나타내므로, 미완료 항목의 성격을 태그로 구분한다.

- `- [ ]` 할 일(열림) · `- [x]` 완료
- `[대기: <조건>]` — 선행·외부 조건(라이브 배포·실기기·도메인 확정 등) 충족 전엔 착수 불가. 우리 판단이 아니라 조건 문제
- `[보류: <이유>]` — 지금은 **의도적으로** 미진행. 우선순위·리스크·비용 판단으로 패스한 것. 체크박스는 열어두되 왜 지금 안 하는지를 남긴다
- `[제외: <이유>]` — 프로젝트 성격이나 기획적으로 맞지 않는 항목, 체크박스를 닫고 그 이유를 남긴다.
- `[코드확인: …]` — 미처리 여부를 코드로 대조한 항목

> 새 항목도 같은 규칙으로 태그. 태그 없는 `- [ ]` 는 "그냥 아직 안 함(하면 됨)".

---

## 공통 · 여러 문서에 반복 (우선순위 후보)

- [x] **"수락 → 재계획" 배선** `[코드확인]` — 알림 카테고리→트리거 매핑을 공유 상수([`REPLAN_TRIGGER_BY_CATEGORY`](../../packages/types/src/inbox.ts))로 올려 인박스 액션·푸시 탭 라우팅 양쪽이 같은 화면(`/planner?tripId=&day=&replan=<trigger>`)에 도착 → planner 비침습 배너 → 트리거 프리필 `ReplanModal`. **자동 재계획은 의도적 제외** — 배너를 닫으면 아무 잡도 안 돈다(CLAUDE.md "추천만" 원칙 유지). `ReplanTrigger` 에 `crowd` 신설(분기는 `Record<ReplanTrigger,…>` 전수 테이블), 재계획 **결과** 알림은 트리거 무관 `replan_ready` 고정해 권유 루프 차단 ([alert-replan-wiring](../alerts/alert-replan-wiring-v1.md)). 배너는 "이 날" 을 말하는데 재계획은 여행 전체이던 어긋남은 [day-scoped-replan](../planner/day-scoped-replan-v1.md)(`targetDays`, 알림 딥링크 일차가 모달 기본 범위)으로 해소. 잔여 항목은 §알림·날씨·혼잡으로 분리
- [ ] **iOS 푸시(APNs) 실기기 검증** `[대기: 실기기 + APNs Auth Key]` — iPhone 17 Pro 시뮬레이터 Debug 빌드·설치·실행과 Firebase plist 번들 포함은 확인(2026-07-22). Auth Key 업로드 + Xcode capability 및 APNs 수신은 실기기에서 검증 ([inbox](../notification/inbox-and-trip-invite-v1.md#L450)·[photo-taste](../preference/preference-photo-taste-analysis-v1.md#L152)·[trip-progress](../trips/trip-progress-live-v1.md#L147)·[mobile](../setup/mobile-webview-setup.md#L226))
- [x] **Web Push (Service Worker + VAPID)** — 브라우저 단독 사용자 푸시 수신 ([web-push](../notification/web-push-service-worker-v1.md)). 자동 권한 프롬프트→옵트인 UI, `platform='web'` 정밀 태깅은 후속
- [x] **DB 마이그레이션 인프라** `[코드확인]` — TypeORM 마이그레이션으로 전환(첫 배포 전이라 프로덕션 데이터가 없어 전환 비용이 가장 싼 시점). [app.module.ts](../../apps/api/src/app.module.ts#L62) 가 `synchronize: isDevelopment` / `migrationsRun: !isDevelopment` 로 개발·프로덕션을 **배타** 분기(둘 다 켜면 synchronize 가 마이그레이션 결과를 덮어씀) → 컨테이너가 뜨면서 스스로 스키마를 맞춰 별도 배포 단계가 없다(replica 1개 전제). 마이그레이션 2건 — 손으로 쓴 `1700000000000-InitVectorSchema`(확장·pgvector 테이블, 타임스탬프를 낮게 고정해 항상 먼저) + 자동 생성 `1785135565704-InitEntities`(엔티티 13개 DDL). CLI 용 [data-source.ts](../../apps/api/src/database/data-source.ts) + `migration:{generate,run,revert,show}` 스크립트. 빈 DB 를 `NODE_ENV=production` 으로 기동해 테이블 17개·확장 2개·HNSW 인덱스 2개 생성 확인 ([deployment §5-2](../ops/deployment-railway-vercel-runpod.md)). 로컬 개발은 `synchronize` 경로 유지 — 기존 DB 에 `migration:run` 하면 `CREATE TABLE` 충돌이라 `docker compose down -v` 후 재생성 ([preferences-enh](../preference/preferences-enhancements-v1.md#L111)·[weighting](../preference/preference-embedding-weighting-v1.md#L94))
- [x] **모달 공통 셸(`ModalShell`) 추출 + 포커스 트랩** `[코드확인]` — [modal-shell.tsx](../../apps/web/src/shared/ui/modal-shell.tsx) 가 body 스크롤 락·ESC·백드롭·포커스 트랩을 전담하고 확인 다이얼로그 6종이 패널 클래스만 넘긴다. 트랩은 [use-focus-trap.ts](../../apps/web/src/shared/lib/use-focus-trap.ts) 로 분리해 자체 애니메이션 phase 를 가진 [BottomSheet](../../apps/web/src/shared/ui/bottom-sheet.tsx) 도 훅만 재사용. `onDismiss` 를 `undefined` 로 넘기면 처리 중 닫힘을 막는다
- [x] **지도 폴리라인 동선 시각화** `[코드확인: 없음]` `[제외: 폴리라인 동선은 오히려 UI 상으로 불편할 수 있음]` — 내 위치 이동 버튼 포함 ([main-planner](../planner/main-planner-v1.md#L263)·[planner-enh](../planner/planner-page-enhancements-v1.md#L124))

## 플래너 · 실시간

- [x] `report-deviation` WS 채널 제거 `[코드확인]` — 송신부 없음 + 수신부는 `ArrivalAlertModule`(서버 스캔)로 대체돼 사문화. 인증만 하고 멤버십은 안 보던 핸들러라 제거가 곧 인가 갭 해소 ([realtime](../planner/realtime-websocket-v1.md#L80))
- [x] replan 워커가 실제 `pushReplanResult` 호출 `[코드확인]` — [alternative.processor.ts](../../apps/api/src/alternative/alternative.processor.ts) 성공·실패 양쪽 호출 + inbox/FCM 폴백
- [x] 게이트웨이 인가: room 재입장/멤버십 변경 시 재검증 `[코드확인]` — 재입장은 `join-trip` 마다 `canAccessTrip` 재검증(기존), 멤버 제거 시 `evictFromTrip` 으로 소켓 즉시 퇴장(신규)
- [x] 트립 레벨 재계획 진입점 `[코드확인]` — 데스크탑 헤더 "AI 재계획" 버튼 + 모바일 FAB → ReplanModal(manual) ([alternative](../planner/alternative-place-picker-v1.md#L142))
- [x] 대안 swap 시 영업시간 위반 경고 `[코드확인]` — 후보 영업시간을 일정 항목 방문 시각과 대조해 `closedAtScheduled` 신호를 카드에 노출(swap 전 경고). `PlannerAlternativeDto.openingHours`/`closedAtScheduled` 추가
- [x] 현재 장소 비교 카드(P3-9) → 대안 카드 취향 근거(reason) 정식 노출로 대체 `[코드확인]` — 좌우 비교 카드는 BottomSheet 세로 레이아웃에 부적합. `place.reason` 을 `waitLabel` 에 욱여넣던 것을 `PlannerAlternativeDto.reason` 전용 필드로 분리해 카드에 한 줄 노출
- [x] pending/resolve 후보 마커 좌표 정규화 일관화 `[코드확인]` — 추천/resolve 응답 마커를 병합 후 `normalizeMarkerPositions` 로 폴백 x·y 재정규화(SDK 미로딩 미리보기 정합)
- [x] 필수 포함 장소 LLM 경로 보장 주입(구 best-effort) `[코드확인]` — `enforceMustInclude` 로 LLM/폴백 계획에서 누락된 필수 장소를 음수 order 로 강제 주입해 일차별 slice 에서 살아남게 보장 ([planner-enh](../planner/planner-page-enhancements-v1.md#L124))
- [x] 일차 간 동선 연속성 미고려 `[제외: 숙소 미모델링 · 일자별 지역]` — 재보니 **잴 대상 자체가 없다.** ① 일자별 지역([per-day-region](../trips/per-day-region-v1.md))으로 1일차=부산·2일차=경주가 되면 경계 거리 100km 는 버그가 아니라 사용자가 고른 값이고, `perDayMode` 는 여행 전체에 지역 2종 이상이면 켜지므로 다지역 여행에선 검사가 전부 오탐이 된다. ② 단일 지역이어도 그 선분은 **아무도 걷지 않는다** — 사용자는 1일차 마지막 → 숙소 → 2일차 첫으로 가는데 숙소가 모델에 없다([toItemType](../../apps/api/src/planner/planner.service.ts#L788) 은 restaurant/cafe/attraction 만 만들고, KTO `contentTypeId=32` 는 [적재 제외](../preference/place-embedding-enrichment-v1.md#L23), 숙소 연동은 [v1 비목표](../overview/product-v1-scope.md#L68)). 실재하지 않는 비용으로 후보를 떨어뜨리게 된다. ③ 고치려 해도 **기준점이 없다** — 전날 마지막 장소를 앵커로 삼는 건 임의 선택이고, 숙소가 시내인데 전날 마지막이 외곽이면 오히려 2일차를 외곽으로 끌어당긴다. 따라서 [constraint.engine.ts:57](../../apps/api/src/planner/constraint/constraint.engine.ts#L57) 의 `current.day !== next.day` 스킵은 누락이 아니라 옳은 선택이며, 정작 필요한 하루 안 이동시간은 같은 코드가 이미 검증한다. 숙소가 데이터에 들어오면 되살릴 항목 ([day-scoped-replan](../planner/day-scoped-replan-v1.md#L136))
- [x] 비연속 일차 범위(`[1,3]`)가 연속 2일로 취급됨 — **두 증상 중 하나는 실재하지 않았다.** ① `영업요일` 은 애초에 문제가 아니다: [isClosedAt](../../apps/api/src/planner/retrieval/opening-hours.parser.ts#L133) 은 시:분만 보고 날짜·요일을 안 쓰며 정본 형식(`HH:MM-HH:MM`)에 요일 정보 자체가 없다. `startAt` 의 시각은 항상 `wakeTime` 이라 `planDays[0]` 이든 다른 날이든 판정이 **동일**하다(`startAt` 소비처는 CRAG availability 항 한 곳뿐). ② 프롬프트 자기모순은 실재: `dayCount: 2` 인데 `startDate`~`endDate` 가 7/10\~7/12(3일)로 나갔다. 시작·종료일 **두 값이 비연속 범위를 표현할 수 없는 게 원인**이라 `PlannerAgentOptions.dayDates: string[]`(인덱스 = 프롬프트 day-1)로 교체하고 프롬프트도 `trip.days: [{day, date}]` + "day 간격을 이어진 하루로 가정하지 말라" 하드룰로 바꿨다. ③ 함께 잡은 별건 — **날씨 힌트가 여행 전체 예보였다**(비연속뿐 아니라 모든 부분 재계획 해당). 2일차만 다시 짜는데 1·3일차 강수가 힌트에 실려 LLM 이 다시 짜지도 않는 날의 비를 피해 실내 장소를 당겼다. `buildWeatherHint(forecasts, dates?)` 로 대상 일차 날짜만 보게 했다. 회귀 5건 추가(592 tests)
- [x] 검색 드롭다운 키보드 내비 · 태블릿 사이드바 접힘 localStorage `[코드확인]` — combobox/listbox + 방향키·Enter·Esc, 사이드바 접힘 상태 localStorage 유지

## 라우팅

- [ ] `departAt` 지원 `[코드확인: 없음]` `[보류: Live 폴링 소비자 부재 + ODsay 기본 API 미지원]` — 심야 대중교통 ETA 오차. searchPubTransPathT 는 출발 시각 파라미터 자체가 없어 캐시키 확장만으론 못 잡고 다른 엔드포인트 전환이 필요 ([routing](../planner/routing-external-api-v1.md#L199))
- [ ] in-flight 병합 Redis 락 `[보류: 단일 인스턴스]` — 스케일아웃 시 인스턴스 간 중복. 그전엔 캐시 TTL 로 bound 되어 순이득 미미
- [x] ODsay 쿼터 폴백 지표화 `[코드확인]` — `RouteHelper.fallback` 단일 통로에서 이동수단·사유별 계수(`getFallbackMetrics`) + 이상 신호만 warn. 쿼터 초과는 전용 코드가 없어 500 계열 `quota_or_server` 버킷으로 잡히며 급증이 곧 신호 ([routing](../planner/routing-external-api-v1.md#L204))
- [ ] `ODSAY_SERVICE_URL` 실제 등록 도메인으로 교체 `[대기: 라이브 배포]`
- (도보 직선거리 추정은 내재적 한계 — 아래 §내재적 한계 참고)

## 알림 · 날씨 · 혼잡

- [x] 예보 악화 시 재알림(선점 키에 확률 저장) `[제외: 알림 피로]` — 재알림은 사용자에게 알림이 잦아 오히려 불편. (여행, 일자)당 1회 유지 ([weather-alert](../alerts/weather-alert-scheduler-v1.md#L130))
- [x] 일차 딥링크(푸시 payload `day` 반영) `[코드확인]` — 세 알림(weather/crowd/arrival)의 open-trip action 에 `day` 주입 → `/planner?tripId=X&day=N` → PlannerView 초기 일차. 스테일 일차는 기존 effect 가 첫 일차로 폴백
- [x] 날씨/재계획 알림 수신 토글 분리 `[코드확인]` — `prefersCategory` 의 weather/crowd/arrival→replan_ready collapse 제거, 각 카테고리가 자기 키를 따름. 설정 UI 는 "재계획 완료"(replan_ready)와 "날씨·혼잡·미도착 추천"(weather_alert 대표, crowd·arrival 동반) 2개 row 로 분리. 기존 replan_ready off 사용자는 이제 추천을 따로 끌 수 있음(추천은 기본 on 으로 복귀)
- [ ] 임계값 캘리브레이션(유예 15분·반경 500m·신선도 10분, 상대 1.2·하한 10%) `[보류: 라이브 데이터 부재]` — 오탐/미탐 지표가 쌓여야 튜닝 근거가 생김. 실데이터 없이는 착수 애매 ([arrival](../alerts/arrival-check-alert-v1.md#L142)·[crowd](../alerts/crowd-alert-scheduler-v1.md#L143))
- [ ] iOS 백그라운드 위치(significant-location-change) `[보류: 실기기 + iOS 네이티브]` — significant-location-change 는 실기기 검증 + 네이티브 작업 필요
- [x] 미도착 배너가 현재 위치를 안 실어 보냄 — **배너까지 스레딩하는 대신 서버가 채운다.** 위치는 이미 미도착 판정용으로 Redis 에 있으므로([LiveLocationService.getFresh](../../apps/api/src/arrival-alert/live-location.service.ts)) [ReplanningService.enqueue](../../apps/api/src/replanning/replanning.service.ts) 가 잡 데이터에 싣는다 — **웹만 고치면 앱에서 안 먹는다**(RN 은 네이티브가 위치를 보고해 웹뷰 JS 에 좌표가 없다). 잡 데이터에 박으므로 워커가 늦게 돌아도 "요청한 순간의 위치"가 유지되고, 웹은 권한을 다시 물을 필요가 없다. **가드 셋** — ① 클라이언트가 보낸 좌표가 있으면 그게 정본 ② `deviation` 트리거만(manual·weather·crowd 는 여행 전 집에서도 눌리는데, 카카오 반경 앵커가 걸리면 후보 풀이 여행지가 아니라 사용자 주변으로 끌려간다) ③ 신선도 10분(`LOCATION_STALE_MS` 공유) + 대상 일차 장소에서 `REPLAN_LOCATION_MAX_DISTANCE_M`(30km, 카카오 기본 반경 20km 보다 크게) 이내. **거리 판정은 `targetDays` 가 가리키는 일차만 본다** — 1일차 근처에 있으면서 2일차를 다시 짜면 그 일차 후보가 엉뚱한 지역으로 앵커된다. 위치 없음·좌표 없어 판정 불가·임계 초과는 모두 조용히 위치 없이(기존 동작) 보내고 초과만 warn. **부수 효과로 `LiveLocationService` 를 [LiveLocationModule](../../apps/api/src/arrival-alert/live-location.module.ts) 로 분리** — `ArrivalAlertModule` 을 통째로 import 하면 TripMembers↔Inbox 순환에 걸려 **부팅이 깨진다**(타입체크·유닛테스트는 통과하고 AppModule 실기동에서만 드러남, `POST /live/location` 라우트 생존까지 확인). 브라우저 단독 사용자는 `/trip/live` 를 벗어나면 보고가 멈춰 10분 뒤 진입엔 앵커가 안 걸린다(RN 은 백그라운드 보고로 계속 신선) — 알림 직후 진입이 정상 경로라 남겨둠. 회귀 9건 ([alert-replan-wiring](../alerts/alert-replan-wiring-v1.md#L128))
- [x] `deviatedItemId` 죽은 필드 제거 — DTO 2곳(replan·schedule-change)·`ReplanRequestDto`·`GenerateOptions` 에서 삭제. 받아서 옮기기만 하고 **읽는 코드가 없었고**, 보내는 코드도 웹·앱·테스트 어디에도 없었다. 남겨두는 비용이 실재한다 — Swagger·타입에 있으니 "지원되는 입력"으로 읽혀서, 직전 백로그 항목이 "인박스 액션에 itemId 를 실으면 해결"이라고 잘못 적혀 있었다(실제로는 그 값이 버려진다). **소비처를 만드는 쪽을 택하지 않은 이유**는 이 필드가 진짜 필요한 기능의 입력이 아니기 때문 — 아래 "아침부터 다시 짠다" 항목이 요구하는 건 항목 ID 가 아니라 시작 시각 앵커다. 되살릴 땐 5줄
- [x] 이탈 재계획이 하루를 아침부터 다시 짠다 — **오늘 일차에 "시작 시각 앵커"를 넣었다.** 15:00 요청이 `currentAt = wakeTime`(08:30)부터 하루를 채워 지난 시각에 일정이 박히고 검색 `startAt` 도 아침 기준이던 것을 `resolveDayAnchors`(planner.service.ts)가 일차별로 갈랐다 — **오늘(KST)인 일차만** 앵커하고 나머지 일차·기상 전 요청은 기존 경로 그대로다(자정 넘는 취침도 벽시계 비교로 맞다). 시작은 `지금 + 10분`(방문 중 항목이 있으면 그게 끝난 뒤)이고, 첫 항목엔 **현재 위치→그 장소 이동시간**을 더한다(앞 항목에서 서버가 채우는 그 좌표를 쓴다 — 빼면 "지금 이미 도착해 있다"를 가정한다). 검색 `startAt` 도 앵커 시각이라 아침에 닫힌 곳·저녁에만 여는 곳 판정이 뒤집힌다. **개수는 강도별 최소치(3/4/5)를 일부러 보장하지 않는다**(`itemsFittingRemaining`) — 남은 2시간에 4곳을 넣으면 ScheduleConstraint 가 뒤 항목을 취침 직전으로 몰아 이동시간 위반이 나고 결정적 폴백까지 실패해 **요청 자체가 죽는다**. 같은 이유로 앵커된 일차는 체류시간을 남은 시간에 맞춰 줄이고 45분도 안 남으면 그 뒤 항목을 버린다. **보존 규칙** — 이미 끝난 항목은 남기고(오늘 다녀온 기록이 사라지면 안 된다), 진행 중인 항목은 **현재 위치가 그 좌표 반경(`ARRIVAL_RADIUS_M` 재사용) 안일 때만** 남긴다(방문 중). 진행 중인데 멀면 다시 짜는 쪽이 맞다 — 그게 미도착이고 이탈 재계획 요청 자체다. 보존 항목은 order 앞자리를 유지하고 후보에서도 빼 같은 장소가 오늘 두 번 배치되지 않는다. 남은 시간에 한 곳도 못 담는 일차는 **손대지 않고**(저장된 일정을 지우고 빈 하루를 남기는 게 최악) 전 일차가 그러면 LLM·검색도 부르지 않는 no-op. LLM 에는 일차별 `startTime`·`targetItems` + 앵커 전용 하드룰 3개를 싣는다 — 안 넘기면 아침 리듬(브런치 카페→점심→오후 명소)으로 짠 뒤 앞에서 잘라 **저녁 재계획에 아침 슬롯이 남는다**. 덤으로 `deviatedItemId` 를 되살릴 필요가 없음이 확정됐다(미도착 항목은 "진행 중 + 멀다" 규칙으로 자연히 다시 짜인다). 회귀 17건(654 tests)
- [x] replan jobId 중복 제거가 트리거별로 갈림 — **시간 창 키를 dedup 의 본체 자리에서 내렸다.** 트리거를 빼는 것만으론 절반만 고쳐진다: 재계획 한 건은 LLM 때문에 분 단위인데 창은 10초라, "왜 안 바뀌지" 하고 다시 누른 요청은 트리거가 같아도 그대로 또 돌았다(결과가 두 번 덮이고 완료 알림도 두 번). 그래서 [`findInFlight`](../../apps/api/src/replanning/replanning.service.ts) 가 등록 전에 큐(waiting·active·delayed·waiting-children)를 훑어 **같은 여행에서 대상 일차가 겹치는 잡**이 있으면 새로 등록하지 않고 그 잡을 돌려준다. **트리거는 안 본다** — 무엇 때문에 눌렀든 같은 일차를 다시 짜는 작업이면 중복이다. 반대로 겹치지 않는 일차(1일차 진행 중 + 2일차 요청)는 별개 작업이라 통과시킨다(일부만 겹치는 `[1,2]`+`[2,3]` 은 겹침으로 본다 — 3일차를 살리려 새 잡을 만들면 2일차가 두 번 재생성된다). **잡을 합치면 사용자에게 말해야 한다** — 이번 요청의 note·필수 장소는 버려지므로, 그대로 "AI가 다시 짜고 있어요" 를 띄우면 반영된 줄 알고 결과를 기다린다. `ReplanJobDto.deduped` 를 새로 내려 planner 토스트를 "이미 재계획이 진행 중이에요"로 가른다. **jobId 는 남기되 레이스 가드로 축소** — 조회~`add` 사이에 끼어든 동시 제출은 양쪽 다 큐가 비어 보이므로, 트리거를 뺀 `${tripId}-${scope}-${bucket}` 이 그 창만 막는다(범위는 유지 — [day-scoped-replan §4.6](../planner/day-scoped-replan-v1.md) 이 정한 "1일차 → 곧바로 2일차" 보호). 큐 조회 실패·무응답(10초)은 "진행 중 없음" 으로 보고 그냥 등록한다(최악이라도 예전과 같은 상태), 조회 후 끝나 버린 잡도 등록으로 흘려보낸다(이미 지나간 WS 결과를 영영 기다리게 두지 않는다). 회귀 10건(664 tests) ([alert-replan-wiring §8](../alerts/alert-replan-wiring-v1.md))
- [ ] 재계획 트리거 검색 키워드에 `맛집` 계열 부재 `[보류: 후보 풀이 실제로 바뀌는 동작 변경]` — weather·deviation·crowd 셋 다. 후보 풀에 restaurant 가 얇아 식사 슬롯이 빌 수 있다. `TRIGGER_KEYWORDS` 에 NOTE 로만 남겨둠
- [x] KTO `tAtsNm` 이름 매칭 누락 지표화 `[코드확인]` — 조용히 사라지던 관광지 조회 스킵을 사유별(region_unresolved·budget_exhausted·no_data·name_mismatch·empty_rate) 집계 → 스캔 끝에 커버리지 요약 로그(스킵 있으면 warn). `fetchConcentration` 이 사유 반환(`ConcentrationLookup`), `CoverageMetrics` 누적. **이름 매칭 로직 개선(부분일치·별칭)은 지표 보고 판단** — 오알림 방지 스킵 동작은 유지 ([crowd](../alerts/crowd-alert-scheduler-v1.md#L143))
- [x] 강수확률 UI 노출 `[코드확인]` — 날씨 카드에 일자별 최대 POP(물방울 아이콘 + %) 노출. 단기예보만 POP 가 있어 중기·폴백 일자는 숨김. 습도는 `[제외: 불필요]` ([weather-forecast](../alerts/weather-forecast-v1.md#L88))

## 취향 · 임베딩

- [x] confidence 활용(CRAG 보정/임계 필터) `[코드확인]` — confidence 0.35 미만 태그는 검색·CRAG 매칭에서 제외하고, 유효 태그 점수는 confidence 만큼 중립값에서 보정 ([photo-taste](../preference/preference-photo-taste-analysis-v1.md#L152))
- [x] 미분석 사진 전용 재분석 버튼 — 자동 복구가 "다음 업로드"에 편승하는 구조(`pendingPhotos` 가 스트랜디드 사진을 새 잡에 같이 태움)라 **10장을 다 채우면 사용자가 하나를 지우기 전까지 복구 기회가 없던** 막힘. [POST /preference-analyzer/reanalyze](../../apps/api/src/preference-analyzer/preference-analyzer.controller.ts) 가 업로드 없이 결과 없는 사진만 태우되 "미분석" 판정은 업로드 경로의 `pendingPhotos` 를 그대로 재사용해 정의가 한 곳뿐이다. **화면엔 셀 신호가 아예 없었다** — `tags: []` 하나로는 '취향 못 찾음'과 '아직 분석 안 됨'이 구분되지 않아 실패한 사진을 "뚜렷한 취향을 찾지 못했어요"로 잘못 말하고 있었고, 버튼도 몇 장인지 알 수 없다. `PreferencePhotoTagsDto.analyzed` 를 따로 내려 갈랐다(행 문구도 분리). **중복 방지가 선택이 아니다** — 버튼은 몇 번이고 눌리는데 **분석 중인 사진도 결과가 없어 pending 으로 잡히므로**, 검사 없이는 장당 35초짜리 vision 호출이 그대로 두 배가 된다(추론 서버는 한 대). `findActiveJob` 이 미완료 잡을 찾아 있으면 그 잡을 돌려주고, Redis 무응답 시 `getJobs` 도 매달리므로 `withTimeout` + 조회 실패는 '없음' 취급(뒤이은 enqueue 가 503 — 한 요청에서 두 번 기다리지 않게). 실동작 검증 — 사진 2장 중 1장 `photoTags` 를 지운 뒤 버튼 노출 → 클릭 → 진행 배너 → 잡 완료로 태그 4개 기록, 진행 중 재요청 2회 모두 같은 jobId, 미분석 0장이면 400 ([photo-taste](../preference/preference-photo-taste-analysis-v1.md#L152))
- [x] 검색 품질 평가 하네스(golden set) → blend weight·radius 튜닝 — `pnpm eval:retrieval` 이 실제 파이프라인(pgvector+카카오 폴백+네이버 인지도)을 그대로 태워 골든셋 10케이스에 recall@k·MRR·지역정합을 잰다. **설계 핵심은 `cat`(적재 커버리지)을 따로 재는 것** — 안 그러면 적재가 얕은 지역의 낮은 recall 을 랭킹 탓으로 오독한다. `--sweep=KEY=v1,v2` 로 환경변수 조합 반복 측정. **결론: blend weight 는 0.6 유지** — 0~1 전 구간에서 R\|cat 0.27~0.30 으로 케이스 1~2건 차이 안에서만 흔들려 바꿀 근거가 없다. 정답의 절반이 카탈로그에 없는 상태(커버리지 47%)에서 상수를 만지면 노이즈를 쫓는 셈이라 커버리지가 먼저다. radius 는 10케이스 중 카카오 폴백이 1건뿐이라 신호 자체가 안 나옴 ([region-filter-and-eval](../preference/place-retrieval-region-filter-and-eval-v1.md))
- [x] ANN 스케일 region 코드 pre-filter — `destination_region ILIKE '경상북%' OR name/address ILIKE …` 를 정본 코드 등가 비교(`region_code`·`sigungu_code` + btree)로 교체. ILIKE 는 인덱스를 못 타 전체 스캔이거나 HNSW post-filter 로 밀려나고, 후자는 지역이 선택적일수록 근사 이웃이 통째로 걸러져 **결과가 조용히 비는** 방향으로 망가진다. 9.7천 행에서 계획이 Seq Scan(17.7ms) → BitmapOr(3.2ms)로 바뀌었고, 이제 스캔이 카탈로그 전체가 아니라 그 지역 크기에만 비례한다. 코드는 적재·질의가 [region-code.ts](../../apps/api/src/planner/retrieval/region-code.ts) 같은 함수로 계산하고 **주소가 라벨보다 우선** — KTO 시도 목록이 `전남광주통합특별시` 같은 통합 행정명을 주는데 그걸 따르면 광주 장소가 전남으로 묶여 '광주' 검색에서 사라진다. 지역정합 99%·금지어 0 으로 누수 없음 확인
- [x] 전국 재적재(현재 경상북도만 검증) — 16개 시도 **9,761행**. 1차 `--reseed --max=50`(provenance 없는 옛 벡터 1,906건 폐기 → 현재 모델로 재생성 + 영업시간), 2차 `KTO_FETCH_OPENING_HOURS=false --max=300`(커버리지 확장, 신규 8,095). **영업시간은 장소당 1콜이라 커버리지 패스와 갈랐다** — 안 그러면 일일 예산 900을 300건에 다 쓴다. 남은 구멍은 §후속의 대표 명소·광주 두 건
- [x] 대표 명소가 카탈로그에 없음 — 네이버 추천 글 언급을 발굴해 적재하는 `popular` 소스 신설. **런타임 인지도 신호는 방향이 반대라 못 쓴다** — 후보 name 이 코퍼스에 몇 번 나오나 세는 역방향 매칭이라 카탈로그에 없는 명소는 셀 대상 자체가 없다. 적재는 방향을 뒤집되 한글 NER 불안정성은 그대로이므로 **정확성을 추출이 아닌 두 관문에 위임** — ① 카카오 키워드 검색이 정본화 ② 그 정본명을 역방향 인덱스로 되짚어 실재 확인('여행'→'경주여행사' 는 정본명 언급 0 으로 사망). 덕분에 추출 정밀도는 카카오 호출 수에만 영향. 명소·맛집 축 분리(한 코퍼스면 빈도 상위가 전부 명소), 후보 깊이 배수 5(3 에선 설악산 129위·서문시장 109위가 상한 밖), 시군구 단위 타깃 허용(강원도 코퍼스에서 설악산은 8회·129위 — 설악산 글은 '속초 여행'으로 쓰인다). **전국 16개 시도 + 골든셋 5개 시군구 실적재로 커버리지 47%→80%, recall@5 0.084→0.124, MRR 0.381→0.633** ([popular-place-ingestion](../preference/popular-place-ingestion-v1.md))
- [x] 광주가 전남으로 묻힘 — 원인은 KTO 라벨이 아니라 **카카오 주소 정본까지 `전남광주통합특별시`** 인 것(행정구역 통합이 주소 체계에 반영). `toSidoCode` 접두 매칭이 '전남'에 먼저 걸려 광주 장소가 전부 전남으로 적재되고, 등가 비교 검색이라 '광주' 목적지가 **조용히 빈다**. `MERGED_SIDO_LABELS` 표 + 시군구 접미사로 분리(광주는 자치구만·전남은 자치구 없음), 적재 검증은 포괄 시도 전체 인정, 백필 마이그레이션(주소가 통합 라벨인 행만·스키마 변경 없음). **판정 불가(주소 없음)일 때 null 이 아니라 fallback 이어야 한다** — null 은 '지역 라벨 없는 행' 의미라 모든 목적지 후보로 살아나고 시군구 코드에 라벨이 새어 들어갔다(10,576행 TS 대조가 이걸 잡음, 최종 불일치 0). **평가 하네스도 같은 버그** — 첫 토큰만 `toSidoCode` 로 봐서 광주 케이스 지역정합을 0%로 오보고, `placeRegionCodes` 로 교체해 99% 복구. 광주 3건 → **321건**, 골든셋 `gwangju-culture` 추가(지역정합 100%·금지어 0) ([merged-sido-label](../preference/merged-sido-label-v1.md))
- [x] 세종 `sigungu_code` 44건 오류 — 원인은 옛 백필 SQL 의 시군구 정규식에 **토큰 끝 앵커가 없던 것**. `(\S*[시군구])` 는 시/군/구 글자가 토큰 중간에만 있어도 거기서 잘라내 `'세종특별자치시 장군면 …'`→`'장'`·`'국책연구원3로'`→`'국책연'`·`'금남구즉로'`→`'금남'` 을 박았다(세종은 시도 아래가 읍·면·동이라 시군구 계층이 없다). TS 정본은 둘째 토큰이 시/군/구로 **끝날 때만** 시군구로 봐서 null — 즉 적재 경로와 백필 경로가 같은 주소에 다른 값을 쓰고 있었다. 그래서 보정의 핵심은 **SQL 로 규칙을 또 쓰지 않는 것** — `pnpm rederive:region-codes`(dry-run 기본, `--apply` 로 갱신)가 행을 읽어 `placeRegionCodes` 에 그대로 넣고 결과가 다른 행만 고친다. 규칙이 한 곳뿐이라 드리프트가 원리적으로 없고, 앞으로 별칭·통합 라벨 표가 바뀌어도 같은 명령으로 카탈로그를 맞춘다. 마이그레이션이 아닌 이유는 [cleanup:catalog](../../apps/api/src/scripts/cleanup-catalog.ts) 와 같다(스키마가 아닌 파생 데이터 보정이고, 재적재하면 `upsertPlace` 가 매번 재파생해 저절로 맞는다). 잘못된 정규식은 마이그레이션에서도 고쳤다 — 이미 적용된 DB 는 다시 돌지 않지만 규칙이 틀린 SQL 을 복붙 근거로 남길 이유가 없다. 로컬 10,481행 재파생에서 `sigungu_code` 44행 교정 · `region_code` 불일치 0(적재·질의 파싱 parity 확인) · 재실행 0행(멱등). **검색 결과는 안 바뀐다** — `'장'` 은 어떤 목적지 파싱에서도 나오지 않는 죽은 코드였다 ([merged-sido-label §6](../preference/merged-sido-label-v1.md))
- [x] popular 패스가 짧은 일반어 상호를 통과시킴 — 2글자(`다른`·`이유`·`이젠`)는 직전 랭킹 튜닝의 `MIN_COUNTABLE_LENGTH=3` 으로 이미 막혀 있었고, 남은 3글자 이상(`조금더`·`맛있게`)을 **지역 특이도 관문 ③**으로 잡았다. 길이 하한·일반어 사전이 답이 아닌 이유는 데이터에 있다 — 3글자 이하 이름이 1,487행이고 대부분 `가우도`·`무등산` 같은 실제 명소이며, 사전은 카페 `담다`·`연다` 처럼 끝없이 늘어난다. **일반어는 어느 지역 코퍼스에서나 같은 비율로 나오고 실제 장소는 자기 지역에만 몰린다** — 지역명 없는 전국 추천 코퍼스를 대조군으로 두고 언급률 비를 본다(실측으로 두 무리가 갈린다: 실제 장소 최저 6.0 vs 일반어 최고 4.0 → 임계 5). 적재는 탈락(`unspecific` 계수 로그), 런타임은 중립(감점 아님 — 그 자리는 '언급 없는 마이너 장소' 의미로 이미 쓰임). 공백 제거 매칭이 본문 '조금 더'를 이름 `조금더`로 세던 누수도 함께 막았다(3글자 이하는 공백 살린 코퍼스만) ([catalog-name-quality](../preference/catalog-name-quality-v1.md))
- [x] 카탈로그에 있는데 상위에 못 오는 랭킹 문제 — 원인 다섯 갈래를 순서대로 측정하며 잡았다. **가장 큰 건 `selectTopDiverse` 가 다양성 상한에 걸린 고득점 후보를 영구히 버린 것**(상한 검사와 자리 채우기를 한 루프에서 해 백필 루프가 실행될 일이 없었다) — 제주에서 점수 3위 한라산·5위 비자림이 사라지고 더 낮은 카페가 그 자리에 들어왔다. 제약 위치도 틀렸다(머리 고정 → 풀 최소 보유량: 플래너는 순서보다 구성이 필요). 인지도 매칭은 양방향으로 틀리고 있었다 — 2글자 상호가 코퍼스 흔한 단어를 훔치고(식당 '다시'가 부사 '다시' 6회로 1위), 장식적 등록명은 통째로 매칭이 안 됐다('대구 서문시장 & 서문시장 야시장' → 토큰 '서문시장'). 코퍼스 30→100건(호출 수 동일·페이지만 큼)이 투입 대비 효과 최대. 취향 블렌드 0.6→0.85(개인화는 taste 태그·CRAG taste 항으로 유지). **하네스 자체도 두 군데 틀렸다** — 짧은 이름 부분일치 오탐으로 지표를 부풀리고(식당 '다시'를 '김광석다시그리기길' 정답으로 인정해 MRR 1.00), 프로세스 내 캐시로 스윕이 조용히 무효. 동일 하네스 기준 **R|cat 0.164→0.375, recall@10 0.122→0.253, MRR 0.470→0.674** ([ranking-tuning](../preference/retrieval-ranking-tuning-v1.md))
- [x] 서울처럼 후보가 밀집한 지역의 풀 진입 — 후보 풀을 `limit×3`(48) → `×10`(160)으로. **후보 선발은 순전히 벡터 거리이고 인지도는 그 뒤 재정렬에만 관여하므로, 49번째부터는 인지도가 1.00 이어도 파이프라인에 못 들어온다** — 서울 660행에 청계천·북촌한옥마을이 밀려 있었다(서울 R|cat 0.00→0.50). **비용은 거의 없다** — 지역 pre-filter 가 이미 그 지역 전체 거리를 계산하므로 LIMIT 은 top-N 힙 크기만 바꾼다(660행에서 48→320 이 31kB→60kB, 5~6ms 동일). 후보가 적은 지역은 LIMIT 이 상한일 뿐이라 **"밀집 지역만" 조건 분기가 불필요**했다. ×20 은 R|cat 0.507(+0.009) 대신 MRR 0.735→0.689 로 노이즈가 함께 들어와 ×10 이 무릎. 전체 **R|cat 0.375→0.498, recall@10 0.253→0.317, MRR 0.674→0.735, 지역정합 100%** ([ranking-tuning §3.7](../preference/retrieval-ranking-tuning-v1.md))
- [ ] 인지도를 후보 선발 단계에 얹기 `[보류: 지금은 이득 없음, 규모 커지면 카드]` — **프로토타입으로 측정하고 되돌렸다.** 런타임 코퍼스에 적재용 forward extraction 을 돌려 이름을 뽑고(인지도 인덱스는 언급 수만 세고 이름 목록이 없다) `name ILIKE ANY` 로 카탈로그를 직접 조회, 벡터 후보와 합집합. 메커니즘은 동작한다 — 벡터 상위 160 밖 후보를 케이스당 2~21건 끌어온다. **핵심 발견: 풀 배수의 대체재다** — 풀×3 에 선발만 얹으면 풀×10 과 동등(R\|cat 0.502 vs 0.498, recall@10 0.326 vs 0.317)한데 160개를 훑는 대신 이름 30개만 조회한다. 다만 **이미 ×10 을 쓰는 지금은 순수 손해**(R\|cat·recall 불변, MRR 0.720→0.674). 하락 원인은 카탈로그에 남아 있던 SEO 상호('경주맛집' 인지도 1.00)를 선발이 증폭한 것 — 아이디어의 결함이 아니라 데이터 품질 문제이고, 그 데이터는 이후 §카탈로그 이름 품질에서 삭제했다(재측정하면 결과가 달라질 수 있음). 카탈로그가 10만 행대가 되거나 HNSW 경로로 밀려 정확 KNN 을 못 쓸 때 꺼내 쓸 카드로 남긴다(그때는 인지도를 행에 저장하는 설계가 런타임 추출보다 나을 수 있다) ([ranking-tuning §6](../preference/retrieval-ranking-tuning-v1.md))
- [x] 카탈로그의 SEO 상호·여행코스 글 행 — 두 부류를 다른 근거로 갈랐다. **SEO 상호는 "단일 어절 + 길이 ≤6 + SEO 접미"** 로 좁혀 7건 적출(오탈락 0 — 코스명은 전부 다어절이라 안 걸린다), 적재는 소스 합류 후 한 곳에서 막고(popular 관문을 막아도 카카오 주변 검색이 '경주맛집'을 다시 주워 온다) 검색 적격성에도 넣었다(실존 음식점이라 카테고리 화이트리스트로는 안 걸림). **여행코스 기사는 이름 모양만으로 판정하면 안 된다** — 어절·조사 규칙으로 걸리는 38건 중 절반이 '경산 임당동과 조영동 고분군'·'강진 병영마을 옛 담장' 같은 실제 명소다. 그래서 KTO 에 시도별 `contentTypeId=25` 목록을 되물어(전국 1,040건) 확정 집합과 교차한 뒤에만 이름 규칙을 적용 → 카탈로그 교차 120건 전부 기사로 판정·삭제, `해파랑길 2코스` 류 42건은 25가 아니라 애초에 대상 밖. 정리는 마이그레이션이 아니라 `pnpm cleanup:catalog`(dry-run 기본) — 삭제는 스키마가 아니고 재적재하면 되살아나므로 게이트가 정본이다. 총 127건 삭제(10,608→10,481), R\|cat 0.498→0.505 · MRR 0.705→0.720 ([catalog-name-quality](../preference/catalog-name-quality-v1.md))
- [x] `retrieval` 항이 사실상 상수 — **가중을 0.24→0.06 으로 내리는 것이 답이었다. 변별력을 만들어 주는 안은 재보니 손해였다.** 먼저 분포를 쟀다([measure-retrieval-similarity.ts](../../apps/api/src/scripts/measure-retrieval-similarity.ts) 는 실제 `retrieve()` 를 태우고 `searchByEmbedding`·`rank` 반환을 가로채 질의 구성·블렌드·지역 필터가 운영과 동일한 풀을 본다) — 코사인 p5 0.433\~p95 0.589, 사상 0.716\~0.794, 실효 스프레드 **0.019**(popularity 는 가중 0.12 로 0.102). **좁은 밴드를 펴는 안은 두 번 막혔다** — ① 풀 내 정규화는 질의마다 기준이 달라 절대 confidence 를 못 만들어 정렬 전용 `rankScore` 분리까지 만들었는데, 스윕 전 구간에서 손해였다(R\|cat w=0.06 0.575 · w=0.24 0.447 vs 미정규화 0.585). 원인은 방식이 아니라 **신호 서열** — 항목별 AUC 가 popularity 0.838 > retrieval 0.656 이고 retrieval 은 케이스 편차도 커(11 중 3케이스가 0.5 이하) 벡터 순서에 발언권을 주는 만큼 더 좋은 신호가 밀린다. 후보 선발이 이미 `ORDER BY embedding <=> query` 라 같은 신호를 두 번 쓰는 것이기도 하다. ② 전역 고정 곡선은 **밴드가 질의마다 이동**해서 불가(경주 0.541\~0.608 과 서울 0.421\~0.516 이 겹치지 않는다 — 케이스 섞은 AUC 0.584 < 케이스별 평균 0.616 이 그 오프셋이 노이즈라는 증거). 채택한 0.06 은 무릎이다(0 이면 R\|cat 0.566·MRR 0.795 로 되돌아가 항 자체는 필요). **남은 몫은 비례 배분해 합 1 을 지킨다** — 안 그러면 모든 confidence 가 통째로 내려가 accept 게이트가 대량 탈락시키므로, 이 배분이 있어야 `CRAG_RETRIEVAL_WEIGHT` 가 게이트를 안 흔드는 순수 랭킹 노브가 된다(같은 이유로 게이트의 인지도 감점 되돌림도 상수가 아니라 실효 가중치를 본다). R\|cat 0.585 **불변** · recall@10 0.371→0.381 · MRR 0.788→**0.841** · 나빠진 케이스 없음 — 결과 집합은 그대로고 순서만 개선됐다(스프레드 0.019 항에서 예상되는 크기) ([crag-term-weight-tuning](../preference/crag-term-weight-tuning-v1.md))
- [x] 죽은 가중을 일하는 항으로 재분배 `[측정 후 폐기]` — locality·dataQuality penalty 발동률이 **0/1441**(availability 1.6%, popularity 77.5%)이라 그 가중을 taste·popularity 로 몰아주는 배율 노브를 만들어 스윕했는데, popularity 실효 가중이 0.160→0.304 로 두 배가 되는 동안 **recall@5 0.231 · MRR 0.841 이 소수점까지 고정**이고 가드를 없앤 구간에서만 R\|cat 0.585→0.575 로 내려갔다. **이유는 기계적 — 후보 간 값이 같은 항은 어떤 가중을 줘도 순위를 못 바꾼다**(모든 총점을 같은 양 올릴 뿐). 죽은 가중은 희석이 아니라 offset 이라 옮겨도 나올 게 없고, retrieval 0.24→0.06 이 먹힌 건 그 항이 상수가 아니라 '거의' 상수(스프레드 0.078)였기 때문이다. 남는 효과는 confidence 절대 수준(0.736→0.732)뿐이라 게이트를 흔드는 리스크지 이득이 아님 → 노브 폐기, 비례 배분 유지 ([crag-term-weight-tuning §6](../preference/crag-term-weight-tuning-v1.md))
- [x] `availability` 항 — **제거가 아니라 감점 전용 전환.** 먼저 커버리지 천장을 쟀다: 영업시간은 KTO 출처(4,501행)만 얻을 수 있고 카카오 전용 5,980행은 구글 Places 미채택 이후 **영구 불가** → 구조적 천장 43%(남은 KTO 3,957행은 장소당 1콜·일 예산 900 이라 5일이면 채우지만 천장은 그대로). 즉 **커버리지 확대로는 이 항을 못 구한다.** 그런데 재보니 문제는 죽은 항이 아니라 **편향**이었다 — `없음 0.58 / 열림 0.95` 라 "KTO 에 영업시간이 등록돼 있다"는 사실에 총점 **0.041**(popularity 언급0 감점폭 0.052 에 맞먹음)을 주고 있었고, 그 데이터가 카테고리와 얽혀 카탈로그 실측 가점률이 식당 7.8%·관광지 5.2%·**카페 0%**(1,312행 전원 카카오 출처)였다. 일정엔 카페 슬롯도 필요한데 구조적으로 배제되던 셈. 그래서 `없음·형식불명·시각없음·열림 = 0.58` / `확인된 닫힘 = 0.25` 로 바꿔 가점만 없앴다. **중립값을 예전 다수값 0.58 그대로 둔 것도 의도** — 후보 95%가 이 값이라 올리면 순위는 그대로인데 confidence 레벨이 통째로 올라 accept 게이트·폴백 판정이 함께 움직인다. 검증은 같은 세션 A/B 3회 소수점까지 동일(R\|cat 0.566·MRR 0.836, 오늘 코퍼스 기준), 닫힘 발동률 25건(1.7%) 불변. **하네스가 이 변경에 눈이 없다는 것도 확인** — 골든셋 정답의 영업시간 보유율 4.8% 가 카탈로그 5.2% 와 같아, 근거는 지표 개선이 아니라 무회귀 + 편향 제거 + 하드 검증([ConstraintEngine.checkOpeningHours](../../apps/api/src/planner/constraint/constraint.engine.ts))과의 역할 분리다 ([crag-term-weight-tuning §10](../preference/crag-term-weight-tuning-v1.md))
- [ ] 영업시간 커버리지 백필 `[별 판단이 됨]` — KTO 3,957행 × 1콜(일 예산 900, 적재와 공유) ≈ 5일. 랭킹 이득은 위 항목에서 끊겼으므로 **화면 영업시간 배지를 더 채우고 싶을 때** 하는 일이다. 카페·카카오 전용 식당은 값싼 소스가 생기기 전까지 판정 불가
- [x] 카카오 전용 행 영업시간 KTO 역조회 `[제외: 실측 히트율 6.7%]` — 위 항목이 "영구 불가"로 적은 카카오 5,846행에 남아 있던 **마지막 우회로**(`searchKeyword2` 이름 검색 + 좌표 250m 대조 + `detailIntro2`, [resolveOpeningHours](../../apps/api/src/planner/retrieval/tour-api.service.ts) 를 전량에 돌리는 안)를 무작위 30건으로 실측했다. 성공 **2/30**(관광지 1·식당 1·카페 0)이고 실패 23건이 **searchKeyword2 검색 0건 = KTO 미등록** — 투썸플레이스 인천효성점·커런트커피·갤러리M 처럼 애초에 카카오 소스를 붙인 이유(카카오 전용 장소 보강)에 해당하는 행들이라 구조적이다. 나머지 4건은 이름은 찾았지만 좌표가 딴 곳(`화심` 185km·`보보` 39km)이고 250m 게이트가 오매칭을 정확히 막았다 — **반경을 늘리는 건 답이 아니다.** 성공 2건 중 하나는 `00:00-23:59`(상시 개방)라 제약 역할이 없어 유효 히트는 1/30(3.3%). 비용은 5,846×2콜 = 11,692콜(`searchKeyword2` 별도 일 한도 1,000 → 최소 6일, 실패 건도 콜 소모)인데 수확은 커버리지 5.3%→7.1% → 같은 예산을 KTO 행 백필(1콜/행)에 쓰는 게 상한이 높다. 카카오·popular 쪽은 고칠 대상이 아니라 **소스 특성으로 확정** ([place-catalog-integrity §5](../preference/place-catalog-integrity-v1.md))
- [x] 지역 적재 타깃·reseed 삭제 키·auto-seed 게이트 — 구조 점검에서 나온 "조용히 틀어지는" 결함 셋. (a) `--regions=속초` 처럼 시도로 안 잡히는 지역이 기본 소스(`tour,kakao`)에서 **타깃째 버려져** 카카오·popular 수집까지 0건이었다(주석 의도는 tour 만 스킵) → tour 만 건너뛰고 타깃 유지, 대신 빈 `lDongRegnCd` 로 KTO **전국 조회**가 새지 않게 가드 추가. (b) `deleteRegion` 이 `normalizeDestinationRegion` 결과를 삭제 키로 써서 4개 슬러그 밖 지역이 전부 `'default'` 로 떨어져 **강원 reseed 가 타지역 폴백 시드를 삭제**했고, 라벨 어간 `'강원%'` 은 시군구 타깃 라벨(`'속초'`·`'강릉'` 78행)을 못 잡아 옛 모델 벡터가 잔존했다(reseed 가 막으려던 상황 자체) → 정본 코드(`region_code = ANY`) 추가 + `'default'` 제외, 실 DB 롤백 검증에서 강원 631→711행. (c) auto-seed 게이트가 seed 슬러그 라벨만 세서 `'서울특별시'` 로 적재된 1만 행을 못 보고 매 지역에 시드를 덧칠했고, 슬러그에 없는 목적지는 `DEFAULT_SEEDS`(서울 좌표 가짜 6건, `region_code`·`sigungu_code` 둘 다 null)가 들어가 **모든 목적지 검색의 unlabeled 예외로 영구 잔존**했다 → 게이트를 정본 코드 기준 `countRegionCandidates` 로 교체 + `'default'` 시드 DB 주입 차단(인메모리 폴백이 이미 커버) ([place-catalog-integrity §3.1~3.3](../preference/place-catalog-integrity-v1.md))
- [x] 임베딩 텍스트 라벨 churn · `sigungu_code` 한 글자 · append 커서 단위 — (a) `buildText` 가 지역 신호로 **수집 라벨**을 물어 같은 장소가 타깃마다 다른 `text_hash` 를 갖고 매 실행 재임베딩됐다 → 주소에서 파생되는 정본 코드로 교체(실적재 재실행에서 갱신 0/유지 3). **전량 재임베딩 불필요** — 구·신 텍스트 벡터 코사인 0.993~0.997(대조군 다른 장소 0.498)로 같은 공간이라 혼재가 무해. (b) `toSigunguCode` 가 접미사를 무조건 떼서 `'중구'→'중'` 을 만들어 1,554행이 **읽을 수 없고 시도가 달라도 값이 같은** 죽은 코드였다 → 한 글자만 남는 자치구는 접미사 유지(`'해운대구'→'해운대'` 는 그대로), `rederive:region-codes --apply` 로 1,554행 정정. (c) `ingest_cursors.next_page` 가 그 실행의 배치 크기에 묶여 page 3 이 `--max=100` 이면 200행·`--max=50` 이면 100행을 뜻해 안 읽은 구간을 영구히 건너뛰었다 → 행 오프셋(`next_offset`)으로 전환, 페이지 경계 **내림** 정렬(누락 대신 재확인), 마이그레이션이 기존 커서를 옛 배치 100 기준으로 환산 ([place-catalog-integrity §3.4~3.6](../preference/place-catalog-integrity-v1.md))
- [x] 숙박 후보 유입 · 실행 간 소스 중복 — 둘 다 "데이터만 지우면 재발"하는 부류였다. (a) 숙박 6행은 규칙 이전 잔재지만 **검색 런타임 카카오 폴백**이 `category_group_code` 제한 없이 훑어 `AD5` 를 계속 올렸고(`'속초 관광지'`→`'OO관광호텔'`), `toItemType` 이 accommodation 을 `'attraction'` 으로 접어 **호텔이 '관광지' 일정 항목**이 됐다 → 적격성 게이트에 카테고리 차단(이름·categoryDetail 은 안 봄 — 호텔 안 카페는 통과해야 한다). (b) 적재 dedupe 는 한 실행 안에서만 돌고 DB 조회는 ID 로만 해서 KTO 가 넣은 장소를 다음 실행 카카오가 다시 넣었다 → `findSamePlace`(이름+좌표 250m)로 신규 행 차단. **갱신이 아니라 건너뛰기** — 두 소스의 텍스트가 달라 덮어쓰면 매 실행 재임베딩 churn 이 된다(영업시간만 비어 있을 때 채움). 반경 250m 근거는 동명 쌍 376개 거리 분포에서 400m 까지 전부 소스 교차(같은 장소의 다른 지오코딩)·500m 초과 209쌍은 도시별 동명 장소이고, 잘못 합치면 장소가 사라지는 방향이라 무릎(400m)보다 보수적으로 잡은 것. 실행 내 dedupe 도 좌표 버킷 → 같은 반경으로 통일(버킷은 250m 내 138쌍 중 68쌍을 놓쳐 DB 판정과 갈렸다). 신규 후보마다 도는 조회라 정규화 이름 함수 인덱스 추가(11ms→0.23ms). 정리는 `cleanup:catalog --only=lodging,dup --apply` 로 148행(10,479→10,331), 정리 후 250m 내 동명 쌍 0 ([place-catalog-integrity §3.7~4](../preference/place-catalog-integrity-v1.md))
- [x] `dataQuality` 항 — **제거하고 좌표 검사를 적격성 게이트로 옮겼다.** availability 와 달리 이 항은 **다섯 검사 중 넷이 타입상 도달 불가**였다(`PlaceDto` 가 name·address·coordinates·category 를 필수로 둔다). 카탈로그 10,481행에 결측 0건이라 남는 실효 차이는 id 없는 seed 후보 -0.004 뿐이고, `crag.dataQuality`·`missing-address` 를 읽는 코드도 계측기·하네스 말고는 없었다. **정작 방어가 필요한 건 좌표였다** — 카탈로그에 KTO placeholder 좌표 3행(계남근린공원·관악구민운동장·세종 미술주간이 전부 `19.694, 117.993` = 남중국해)이 있고, 적재 게이트가 non-finite·(0,0)만 막아 통과했다. 지도 마커가 바다에 찍히고 이동시간이 수천 km 로 나온다. `dataQuality` 는 이걸 못 잡는다(좌표가 '있으면' 가점, 값의 타당성은 안 본다) → `isPlausibleKoreanCoordinate`(마라도~고성·백령도~독도 bbox)를 적재·검색 적격성·`cleanup:catalog --only=coords` 세 곳에 넣었다(SEO 상호와 같은 이유로 한쪽만 막으면 다시 들어온다). **상수 항을 빼면 임계도 재보정해야 한다** — 제거로 confidence 레벨이 0.02 내려가 daegu top-8 이 0.648→0.619 로 폴백 임계(0.64)를 넘어 카카오·시드를 부르기 시작했는데 **상위 5개와 지표가 완전히 동일**했다(외부 API 만 추가) → `CRAG_TARGET_CONFIDENCE` 0.64→**0.61** 로 상계. accept 게이트는 최저 0.589 로 여유가 커 유지. 검증: 같은 세션 A/B 에서 R\|cat 0.566·recall 동일, MRR 0.836→0.841, 폴백 0/11 복원, 테스트 +4(579건) ([crag-term-weight-tuning §11](../preference/crag-term-weight-tuning-v1.md))
- [x] 좌표 불량 3행 DB 정리 — `pnpm cleanup:catalog -- --only=coords --apply` 로 삭제(10,481→**10,478**행, 재실행 0건 멱등). **검색 결과는 안 바뀐다** — 지역 pre-filter 가 서울·세종 행을 제주 질의 풀에 넣지 않고 적격성에서도 이미 빠지고 있었다. 재측정에서 recall@5 가 0.231→0.222 로 움직인 건 삭제가 아니라 코퍼스 변동이었다(정답 `비자림` 점수·항목 전부 동일, `새별오름` popularity 0.955→1.000 으로 한 칸 끼어듦) ([crag-term-weight-tuning §11.5](../preference/crag-term-weight-tuning-v1.md))
- [x] 골든셋에 카카오 폴백·방문시각 케이스 추가 — 하네스가 `currentLocation`(좌표 앵커 폴백 + 거리 점수)·`startAt`(영업시간 가드)을 받게 하고 케이스 3개 추가: `yeongju-thin-catalog`(카탈로그 3행 → 앵커 없는 키워드 폴백), `taebaek-deviation-anchored`(3행 + 태백역 좌표 + deviation → 앵커 검색), `sunchang-late-evening`(21:00 KST + limit 8). **개수 지표만으로는 감점을 못 잡았다** — `closedHits` 는 감점 무력화 A/B 에서 양쪽 다 1 이었다(순창 `강천사계곡` 이 3위→5위로 밀릴 뿐 8칸 안에 남는다. 다음 후보와 간격 0.064 vs 감점 0.037) → **순위까지 보는 `firstClosedRank`** 추가로 `1@5`(감점 있음) vs `1@3`(없음)이 갈린다. 판정은 `isClosedAt` 으로 뽑아 점수 계산과 하네스가 공유(규칙이 두 곳이면 계측 대상이 감점 대상과 어긋난다). forbidden 은 **목적지 지역에 동명 장소가 없는 이름만** — `감천문화마을`(부산)을 넣었더니 카카오가 준 경북 예천 동명 마을에 오알람이 떴다(주소 기준 지역정합은 100% 로 옳게 판정). 새 기준선 14케이스: R\|cat 0.659 · recall@10 0.401 · MRR 0.839 · 지역정합 100% · 폴백 2/14(**세트가 바뀌어 11케이스 수치와 비교 불가**) ([crag-term-weight-tuning §12](../preference/crag-term-weight-tuning-v1.md))
- [x] `locality` 가드를 정본 지역 코드로 교체 — **케이스 추가로 드러난 버그를 고쳤다.** `localityScore` 가 `normalizeDestinationRegion`(아는 목적지 서울·부산·제주·경주 넷) 에서 `'default'` 로 빠지면 전 후보가 0.62 라 14케이스 중 10케이스에서 가드가 안 돌았다. 적재·검색 pre-filter 가 쓰는 정본 경로(`destinationRegionFilter`·`placeRegionCodes`)로 교체하고 규칙 셋을 명시 — ① **시도는 시도끼리, 시군구는 시군구끼리**(교차 비교하면 `경기 광주시`가 `광주광역시` 목적지와 맞는 것으로 읽힌다) ② 지역 라벨 없는 후보는 감점이 아니라 **중립**(폴백 시드가 전부 감점되면 안 된다) ③ **한 후보도 안 맞으면 가드를 끈다**(`destinationRegionFilter` 는 '발리'에서도 코드를 만들어 내고, 일률 감점은 순위를 못 바꾸면서 confidence 레벨만 0.06 내려 임계를 흔든다). A/B: 지표 전부 동일(R\|cat 0.659 · MRR 0.827 · 지역정합 100% · 폴백 2/14), locality 중위값 0.62→0.92 · conf 0.690→0.730. **값은 지표가 아니라 임계의 일관성** — 예전엔 비하드코딩 목적지가 체계적으로 '약한 풀'로 판정돼 카카오 폴백을 더 자주 탔다. 관측 안 되는 가드는 테스트로 지킨다(단위 4건) ([crag-term-weight-tuning §14](../preference/crag-term-weight-tuning-v1.md))
- [ ] `personalization` 압축 `[보류: 하네스로 검증 불가]` — retrieval 과 같은 `(sim+1)/2` 압축이 있고 밴드도 0.303\~0.479 로 좁지만 AUC 0.578 로 신호가 거의 없다. 골든셋 정답이 '목적지 유명 명소' 라 개인화 품질 자체를 못 재는 한계 때문일 수 있어, 하네스로 검증할 수 없는 항을 같은 작업에서 건드리면 회귀 원인을 못 가리므로 분리했다. 취향별 정답이 있는 케이스가 생긴 뒤 재개 ([crag-term-weight-tuning §8](../preference/crag-term-weight-tuning-v1.md))
- [x] 검색 결과 근접 중복 제거 — 문제 범위가 제보보다 넓었다. 골든셋 11케이스 상위 16칸 실측에서 **케이스당 1~4칸**이 같은 장소로 낭비됐다(부산 `국립수산과학관`·`다대포해수욕장`·`광안리해수욕장` 각 2행, 광주 `광주양동시장`/`광주 양동시장`(공백만 다름), 경주 `국립경주박물관`+특별전시관+어린이박물관, `첨성대`/`경주 첨성대`). 적재 dedupe 가 **한 실행 안에서만** 돌고 kakao id 가 다르면 다른 행으로 들어오는 게 원인. [near-duplicate.ts](../../apps/api/src/planner/retrieval/near-duplicate.ts) 가 **근거가 다른 두 규칙**으로 접는다 — ① 이름이 같으면(공백·목적지 접두 무시) 거리·카테고리를 안 본다(카드엔 이름만 보이니 좌표가 무엇이든 고를 수 없는 후보다) ② 포함 관계는 2km + 동일 카테고리에서만(`이순신광장`↔`카페모카 힐 이순신광장점` 처럼 **명소 이름을 딴 가게**가 사라지면 안 되므로 카테고리가 방어선, 2km 는 `한라산`↔`한라산 동능` 1.84km 를 잡고 `한라산1100고지` 7.3km 는 남기는 폭). 무리 짓기는 전이적이고 **대표는 최고점이 아니라 최단 이름** — 일정 카드엔 '국립경주박물관 어린이박물관'보다 '국립경주박물관'이 맞다. 접기는 **점수 정렬 뒤**라 대표가 무리 최상위 자리를 유지한다. 이름 하한은 3자(4로 올리면 '한라산'이 '한라산국립공원'을 못 잡아 원래 케이스가 빠진다). R\|cat 0.505→0.573 · MRR 0.720. **카탈로그의 중복 행 자체는 남아 있다**(`경포해수욕장` 좌표까지 동일) — 검색단에서 접으므로 결과엔 안 나오지만 데이터 정리는 별건
- [x] `INDOOR_TAGS` mood/environment 자동화 · `산` 정규식 개선 — `산` 규칙은 **양방향으로 틀리고 있었다**(카탈로그 10,481행 대조). 오탐 34건은 전부 '…산' 으로 끝나는 시·군(`CGV 논산`·`군산 해망굴`·`개심사(서산)`·`금산 칠백의총`) — 옛 규칙이 부산·울산·마산·경산 넉 자만 막아 나머지 열 곳이 샜다. 미탐 46건은 뒤에 한글이 오면 무조건 제외한 탓에 빠진 **가장 대표적인 산악 후보**(`한라산국립공원`·`팔공산케이블카`·`비슬산자연휴양림`)라 시설어 화이트리스트로 회복했다. 행정지명 배제는 **앞 글자가 한글이면 적용하지 않는다** — '금오산'의 '오'를 오산으로 보면 실제 산이 죽는다. **임야 지번(`… 산 182`)은 일부러 남겼다** — 옛 정규식이 의도치 않게 잡던 신호인데 실측 200건 중 198건이 백록담·사라오름·금정산성처럼 이름에 '산'이 없는 실제 산악 후보였고 카페·식당은 2건(둘 다 산속 가게)이라, 버리는 게 손해다. 근거가 다르니 규칙만 갈랐다. 이름에 '산'이 없는 자연 후보용으로 오름(제주 49행)·산악·휴양림·폭포·둘레길 힌트 추가. `INDOOR_TAGS` 는 실내 태그만 담은 배열 → `Record<MoodPreference|EnvironmentPreference, boolean>` 전수 테이블 파생으로 바꿔 어휘가 늘면 컴파일 에러가 나게 했다(판정값 자체는 불변). R\|cat 0.573→0.585 · MRR 0.720→0.788(속초 0.25→1.00). **저장된 임베딩 텍스트는 재적재까지 옛 태그** — 런타임 CRAG taste 항만 즉시 반영 ([weighting](../preference/preference-embedding-weighting-v1.md#L94))

## 트립

- [x] 관광공사 `areaBasedList2` — 전국 일정 카탈로그 `[코드확인]` — 이미 적재 파이프라인에 연결(`TourApiService.fetchByArea`→`areaBasedList2`→pgvector 적재, [tour-api.service.ts](../../apps/api/src/planner/retrieval/tour-api.service.ts#L479)). 리트리벌은 pgvector 우선·seed 폴백. "연동"은 done, 남은 건 적재 커버리지(아래 §시군구 인제스천) ([destination](../trips/destination-tour-api-v1.md#L81))
- [x] 관광공사 `locationBasedList2` — 대안 후보(현재 좌표 주변) `[제외: 카카오+pgvector 로 대체]` `[코드확인]` — "현재 좌표 주변 대안 후보"는 이미 구현됨. `ReplanRequestDto.currentLocation` → `RetrievalContext` → [kakao-local.service.ts](../../apps/api/src/planner/retrieval/kakao-local.service.ts#L75) `search` 가 좌표를 center+radius 로 카카오 주변 검색, [crag-evaluator.service.ts](../../apps/api/src/planner/retrieval/crag-evaluator.service.ts#L141) 가 거리 점수 가점. KTO `locationBasedList2` 는 불필요(CLAUDE.md "대안 후보는 pgvector 취향 유사도로 대체" 방침 일치)
- [x] 영업시간 화면 배지 노출(`PlannerItineraryItemDto.openingHours`) `[코드확인]` — API 응답에 영업시간을 연결하고 일정 카드에 시계 아이콘과 함께 노출 ([opening-hours](../trips/tour-api-opening-hours-v1.md#L147))
- [x] 카카오 전용 장소 영업시간 소스(구글 Places 등) 검토 `[제외: 비용]` — 구글 Places 는 호출 비용이 붙어 도입 안 함. KTO 미등록 카카오 전용 장소(카페·프랜차이즈) 영업시간은 빈 채로 유지(문서 §7 한계 그대로)
- [ ] backfill 스테일 처리(KTO가 영업시간 내린 경우) `[보류: 빈도 낮음·실익 미미]` — 현재 "마지막 확보값 유지". KTO 가 영업시간 필드를 내리는 빈도가 낮아 후순위
- [x] 히어로 카드 날씨 미리보기 `[제외: 불필요]` — 요약 API 마다 격자 변환+예보 조회가 붙어 응답이 무거워지는 데 비해 목록 화면 날씨 미리보기 가치가 낮음 ([main-page](../trips/main-page-filters-card-v1.md#L107))
- [ ] 추천 여행지 서버 캐시(현재 프론트 staleTime만) `[보류: 부하 미발생]` — `DestinationsService.recommend` 는 사용자별 pgvector 집계라 캐시 이득이 있으나, 트래픽 부하가 실제로 생기기 전엔 프론트 staleTime 으로 충분
- [x] 시군구 인제스천 커버리지 확대(현재 경북·대구) — 코드가 아니라 적재 스크립트를 지역별로 돌리는 운영 작업이라 필요 시점에 인제스천 실행으로 커버. 코드 과제 아님
- [ ] 멤버 입력 초대 링크 확장 `[보류: 우선순위·규모]` — 초대 토큰 발급 + 외부 채널이라 규모가 크고, 친구 §"비회원 멤버 직접 초대(이메일)"와 겹침 ([trip-create](../trips/trip-create-v1.md#L195))

## 친구 · 멤버

- [x] 친구 요청 알림 채널(FCM/in-app toast) `[코드확인]` — FCM(`notifyFriendRequest`) + 인박스 목록 실시간 갱신(가상 row 라 `create` 우회 → `pushInboxRefresh` 로 직접 WS 신호, 요청 생성·취소 양쪽) + 전역 인앱 토스트(`inbox_toast` WS → providers `InboxToast`, 탭 시 /inbox) 3채널 완비. 토스트·FCM 은 `friend_request` 토글 따름, 목록 갱신은 토글 무관. 앱 전역 미읽음 배지 실시간은 모든 알림 공통 사안이라 별도로 완료(아래 §인박스·푸시 인프라) ([friends](../friends/friends-and-trip-members-v1.md#L312))
- [ ] 비회원 멤버 직접 초대(이메일) `[보류: 우선순위·규모]` — 초대 토큰 발급 + 메일 발송 + 가입/합류 배선이라 규모가 크고, 미가입자는 FCM 대상이 아니라 외부 채널(이메일)이 별도로 필요. 현재는 핸들 등록(즉시 accepted 합류)으로 갈음
- [x] 조율 `recommendation` ↔ 실 일정 item highlight 연결 `[제외: 기획 범위 밖]` — 조율 추천은 취향 태그 기반 설명 텍스트(`buildRecommendation`)로 충분하고, 이를 실 itinerary item 과 하이라이트로 잇는 건 기획상 불필요. 추천이 item id 를 담지 않는 현 구조 유지
- [ ] `FriendMemberPicker` floating 로직 `@floating-ui/react` 추출 `[보류: 현 구현 동작, payoff 는 shared primitive 통합 시에만]` — 유일 실사용처(friend-member-picker)는 이미 flip·maxHeight 동작해 one-off 교체 이득 미미. 진짜 가치는 shared/ui 에 `Popover` primitive 를 @floating-ui 로 만들어 [place-search-picker](../../apps/web/src/shared/ui/place-search-picker.tsx)·[destination-search-input](../../apps/web/src/features/destination-search/ui/destination-search-input.tsx) 까지 3곳 통합(flip·shift·portal 정합성 + dedup)할 때 생김. 별도 리팩터 과제라 우선순위 낮음

## 인증 · 설정

- [x] 약관/개인정보처리방침/고객센터 실 페이지 `[라이선스는 제외]` — 설정의 앵커 placeholder(`#terms`·`#privacy`·`#contact`)를 실 라우트로 대체. 공통 셸 [document-page.tsx](../../apps/web/src/shared/ui/document-page.tsx)(`DocumentPageShell` = 뒤로가기 헤더 + 섹션 카드, trip-create 헤더와 동형)를 만들고 [/legal/terms](../../apps/web/src/views/legal-terms/ui/legal-terms-view.tsx)(이용약관 11개 조항, AI 추천 면책·콘텐츠 권리 반영)·[/legal/privacy](../../apps/web/src/views/legal-privacy/ui/legal-privacy-view.tsx)(개인정보처리방침, 실 외부 연동처 카카오·ODsay·기상청·관광공사·네이버·FCM·Sentry 명시)·[/support](../../apps/web/src/views/support/ui/support-view.tsx)(고객센터 = 문의 메일 + FAQ 4개)를 정적 콘텐츠로 채움. **라이선스(`#open-source`)는 이번 범위 제외**로 앵커 유지 — 빌드 시 의존성 라이선스 자동 생성(license-checker)이 별도 과제. **남은 건 코드가 아니라 운영** — 문의/개인정보 메일은 도메인 확정 전 임시값(`support@`·`privacy@tripick.place`) + `TODO` 주석이라 §이메일 발신 도메인 확정과 함께 교체, 법무 검토도 출시 전 필요(스토어 심사에 개인정보처리방침 URL 필수) ([settings-view.tsx](../../apps/web/src/views/settings/ui/settings-view.tsx#L70))
- [x] `APP_VERSION` package.json 자동주입 `[코드확인]` — 하드코딩 `'0.1.0'`(실제 package.json `0.0.1` 과 이미 어긋나 있던) 을 제거하고 [next.config.mjs](../../apps/web/next.config.mjs) 가 `apps/web/package.json` version 을 `NEXT_PUBLIC_APP_VERSION` 으로 주입(빌드 시 인라인) → [settings-view.tsx](../../apps/web/src/views/settings/ui/settings-view.tsx#L17) 가 소비. 단일 출처라 버전 올리면 화면도 따라온다. RN 셸 자체 버전은 아직 웹 버전과 별개(네이티브 버전 브리지는 후속)
- [x] 탈퇴 사유 수집 + 2단계 확인 `[soft delete·30일 grace 는 제외]` — 결제·거래 이력이 없어 데이터 보관 의무가 없고, soft delete 로 바꾸면 cascade 사슬(여행·친구·인박스·fcm) 정합성 규칙을 새로 정해야 하는 데 비해 얻는 게 없어 hard delete 유지. 대신 오조작 방지를 절차로 옮김 — `POST /users/me/withdrawal` 이 ① 익명 사유(객관식 7 + 자유입력, 건너뛰기 가능) 수집 후 ② 확인 문구 "탈퇴" 일치 검증을 통과해야 삭제한다. 사유는 userId 없는 `withdrawal_reasons` row(가입 후 경과일만 부가)로 남아 재식별 불가 ([users.service.ts](../../apps/api/src/users/users.service.ts)·[withdrawal-dialog.tsx](../../apps/web/src/features/delete-account/ui/withdrawal-dialog.tsx))
- [x] 디바이스별 푸시 토큰 관리 UI `[제외: 실서비스 관행상 불필요]` — 다중 기기 발송은 이미 동작(사용자 1:토큰 N, `listTokens` 가 전 기기 발송)하고, 죽은 토큰은 발송 실패 시 `remove`·로그아웃/탈퇴 시 `removeForUser`/`removeAllForUser` 로 자동 청소돼 사용자가 손댈 게 없음. 실서비스에서 "푸시 기기 목록 관리" 화면은 거의 없고, 있는 건 보안 목적의 세션/로그인 기기 관리(FCM 토큰 관리 아님). 필요해지면 "이 기기에서 알림 받기" 토글 1개로 갈음하고, 다중 로그인 관리가 필요하면 별도 세션/refresh 토큰 관리 기능으로 설계 ([fcm-token.service.ts](../../apps/api/src/notification/fcm-token.service.ts))
- [x] refresh 토큰 RN SecureStore 이전 `[코드확인]` — WebView localStorage 대신 네이티브 Keychain/Keystore 에 refresh 보관, access 만 웹뷰 유지. 웹=auth HTTP·네이티브=순수 SecureStore, correlation id 브리지 + 타임아웃/확정부재 구분. `pnpm install` + 네이티브 rebuild 후 실기 검증 남음 ([refresh-token-securestore](../auth/refresh-token-securestore-v1.md)·[email-login](../auth/email-login-and-session-v1.md#L163))
- [x] 429 응답 한국어 메시지 + 재시도 UI `[코드확인]` — throttler 기본 본문이 영문(`ThrottlerException: Too many requests`)이라 사용자에게 그대로 노출되던 걸 한국어로 대체([client.ts](../../apps/web/src/shared/api/client.ts)). **남은 초는 메시지에 굽지 않는다** — 카운트다운이 도는 동안 문구가 낡기 때문에, 메시지는 고정하고 `useRetryCountdown`(데드라인 역산이라 인터벌 지연·백그라운드 탭에도 안 밀림)이 초를 세서 로그인·가입·재설정 메일·비밀번호 변경 4개 폼의 제출을 막고 "N초 후 다시 시도" 라벨을 띄운다. `Retry-After` 는 CORS 기본 노출 헤더가 아니라 [main.ts](../../apps/api/src/main.ts) `exposedHeaders` 에 명시해야 크로스 오리진에서 읽힌다 — 빠지면 카운트다운이 조용히 죽음
- [ ] 이메일 인증/재설정 메일 템플릿 정리 `[대기: 발신 도메인 확정]` `[코드확인]` — 템플릿 자체는 이미 완성. [email.service.ts](../../apps/api/src/email/email.service.ts) `buildEmailHtml` 이 브랜드 마크·CTA 버튼·"버튼 안 되면 링크 복사" 폴백·`escapeHtml`·plain text 동시 발송까지 갖춤(문서 작성 시점의 텍스트-only 상태에 백로그가 머물러 있던 것). 브랜드 표기 불일치(from `TriPick` vs 본문 마크 `Tripick`)만 이번에 통일. **남은 건 코드가 아니라 운영** — 발신 도메인 `noreply@tripick.place` 확정 + SPF/DKIM 설정(안 하면 스팸함 리스크). 도메인 확정 시 §모바일 셸 번들 ID 확정과 함께 처리
- [x] 이미지 업로드 클라 다운스케일(webp) `[코드확인]` — 원 항목("프로필 이미지 webp 변환 + 썸네일")을 프로필 단독이 아니라 취향 사진과 묶어 처리. 지난 리뷰의 "256px 하나로 프로필·취향 양쪽 재사용" 은 취향엔 틀렸다 — vision 분석이 해상도를 쓰므로 표시 크기(80px)로 줄이면 태그 추출 품질이 깎인다. 그래서 소비처별로 나눠 [downscale-image.ts](../../apps/web/src/shared/lib/downscale-image.ts) 공통 유틸이 **프로필 256px webp(표시 전용) / 취향 1024px jpeg(vision-safe)** 로 브라우저 canvas 다운스케일한다. **취향은 반드시 jpeg** — 로컬 vision 서버(llama.cpp mtmd=stb_image)가 webp 를 못 읽어 `mtmd_helper_bitmap_init_from_buf` 가 실패하고 ffprobe 폴백까지 떨어진다([profile-image-uploader](../../apps/web/src/features/manage-profile-image/ui/profile-image-uploader.tsx) 업로드 전, [preference-setup-form](../../apps/web/src/features/preference-setup/ui/preference-setup-form.tsx) 분석 잡 전). 서버는 원본 buffer 그대로 저장([users.service.ts:290](../../apps/api/src/users/users.service.ts#L290)) 무변경 — vision 잡이 스토리지에서 다시 읽어 base64 로 밀던 입력([analysis.service:120](../../apps/api/src/preference-analyzer/preference-analysis.service.ts#L120))도 같이 경량화된다. **서버 sharp·별도 썸네일 키는 제외** — 네이티브 바이너리 배포 비용 대비 얻는 게 클라 canvas 와 거의 같고, 아바타 소비처는 하나뿐이라 썸네일이 과하다. 캔버스 미지원·디코드/인코드 실패·결과가 원본보다 크면 원본 폴백(서버가 jpeg/png/webp 다 받음), EXIF 회전은 `imageOrientation` 보정
- [x] 미로그인 공통 가드(모든 nav 페이지) `[코드확인]` — 이미 구현돼 있던 항목. [session-guard.tsx](../../apps/web/src/entities/session/ui/session-guard.tsx) 의 `SessionGuard` 가 nav 5페이지(홈·취향·친구·알림·설정) + planner·trip-create·trip-progress 를, `GuestGuard` 가 login·signup·forgot-password 를 감싼다. [use-session-guard.ts](../../apps/web/src/entities/session/lib/use-session-guard.ts) `redirectWithFallback`(100ms 뒤 URL 미변경이면 하드 네비게이션)이 settings-profile 백로그의 "데스크탑 PC 미로그인 동선 — RN WebView 에서 `router.replace` silently fail" 도 같이 해소. **/reset-password 는 의도적으로 가드 없음** — 로그인 상태로 메일 링크를 눌러도 유효한 일회성 토큰과 명시적 의도를 살려야 해서 `GuestGuard` 를 붙이지 않고, 대신 재설정 성공 시 `clearSession()` 으로 서버가 이미 폐기한(`revokeAllRefreshTokens`) 죽은 로컬 세션을 비운다
- [ ] `Section`/`LinkRow`/`InfoRow` shared/ui 승격 `[보류: 유일 사용처]` `[코드확인]` — 원 문서의 승격 조건이 "다른 페이지에서도 쓰이면"인데 실사용처는 [settings-view.tsx](../../apps/web/src/views/settings/ui/settings-view.tsx) 하나뿐(friends-view·trip-info-panel 의 비슷한 이름은 `SectionLabel` 로 다른 컴포넌트). 순서 문제도 있다 — `LinkRow` 가 가리키는 `#terms`·`#privacy`·`#contact` 가 전부 스텁이라, 위 "약관/개인정보처리방침 실 페이지" 항목이 처리되면 시그니처(내부/외부 링크 구분)가 바뀐다. 지금 승격하면 shared 계약을 두 번 고치는 셈. §친구·멤버의 `FriendMemberPicker` floating 항목과 같은 판단

## 인박스 · 푸시 인프라

- [x] inbox WebSocket invalidate(`inbox:<userId>`) `[코드확인]` — 게이트웨이가 인증 소켓을 `inbox:<userId>` room 에 자동 합류(멤버십 검증 불필요, 본인 채널), `InboxService.create` 가 `pushInboxInvalidate` 로 신호 → FE `useInboxInvalidateSubscription` 이 `inbox.list` invalidate. 브라우저 단독 FCM 공백 보완 ([inbox](../notification/inbox-and-trip-invite-v1.md#L450))
- [x] `trip_reminder`(D-1/D-day) 스케줄러 `[코드확인]` — `NotificationSchedulerModule`(BullMQ repeatable, weather-alert 등록 패턴 복제)이 출발 전날/당일 아침(09:00 KST) 확정 여행 멤버에 `trip_reminder` inbox+FCM 발송. Redis SET NX 로 (여행·종류)당 1회. inbox 액션 매핑이 이미 `trip_reminder→open-trip` 이라 inbox 무변경
- [x] invitee 일정 변경 owner 승인 흐름 `[코드확인]` — 원안("owner 전용 UI 숨김") 대신 **변경 UI 는 owner·참여자 모두 노출 + 비-owner 변경은 owner 승인(알림 동반) 후 반영** 으로 방향 전환. 일정 변경 6종(추가·삭제·수정·순서·swap·AI 재계획)을 범용 제안(`ScheduleChangeProposal`, kind+payload)으로 저장 → owner 가 diff 확인·승인 시 owner 권한으로 replay. 알림은 `trip_invite` 수락/거절 패턴 복제(`schedule_change_request`/`result`). 멤버 추가/제외 UI 는 (제안 대상이 아니라) owner 전용으로 숨겨 원안의 "추가/제외" 도 완결 ([invitee-change-approval](../planner/invitee-change-approval-v1.md))
- [x] owner가 pending 멤버 취소 시 invitee 알림(현재 무음) `[코드확인]` — `TripMembersService.remove`(두 삭제 경로 공통)가 pending invitee 취소 시 `InboxService.cancelTripInvite` 호출 → 남은 trip_invite 카드 삭제(jsonb tripMemberId 매칭) + general "초대 취소" 알림. 접근 불가라 open-trip 액션 없음
- [x] `friendUserId` 없는 핸들 친구 가입 유도 푸시 `[제외: 미가입자 푸시 채널 부재]` — 핸들만 등록된(friendUserId 없는) 친구는 아직 서비스 미가입이라 FCM 토큰이 없어 보낼 대상 자체가 없음. SMS·카카오 알림톡 등 외부 채널이 필요해 현재 푸시 인프라 범위 밖. 현재는 즉시 accepted 합류 유지
- [x] 알림 카테고리별 sub-filter `[코드확인]` — 기존 상태 필터(전체/읽지않음/응답필요)와 직교하는 카테고리 chip 열 추가. 현재 목록에 실제 존재하는 카테고리만 chip 노출(빈 카테고리 숨김), 선택 카테고리가 사라지면 전체로 폴백
- [x] 알림 30일 자동 archive 정책 `[코드확인]` — `NotificationArchiveService`(04:00 KST 스캔)가 읽은 지 30일 지난 알림 hard delete. 미읽음은 나이 무관 보존(못 본 알림 유실 방지), 친구 요청은 friends 가상 row 라 무영향. `synchronize` 의존이라 soft flag 컬럼 대신 삭제 선택
- [x] nav 미읽음 배지 앱 전역 실시간 `[코드확인]` — 기존 `inbox_invalidate` 구독이 inbox-view 에만 마운트돼 다른 페이지 배지가 안 갱신되던 공백 해소. `subscribe-inbox-unread`(세션 게이팅 인박스 요약 조회 + 전역 `inbox_invalidate` 구독)가 미읽음 수를 추적해 providers 에서 앱 트리에 주입, 하단 탭·데스크탑 nav 알림 아이템에 배지 노출(하단 9+·데스크탑 99+ 축약). shared(nav)는 `InboxBadgeProvider` context 계약만 두고 값은 상위 feature 가 채워 순수 유지(FSD). 쿼리 키가 inbox-view 와 같아 캐시 공유(중복 fetch 없음)

## 모바일 셸

- [ ] iOS/Android 번들 ID·applicationId 실도메인 확정 `[대기: 서비스 도메인 확정]` ([mobile](../setup/mobile-webview-setup.md#L226))
- [ ] release keystore 분리(현재 debug fallback) `[대기: 라이브 배포]`
- [x] WebView 첫 로드 실패 시 retry UI `[코드확인]` — 첫 네트워크/HTTP 오류에 안내와 재시도 버튼을 표시하고 WebView remount 로 복구
- [ ] 웹뷰 파일 선택 사진 업로드 — 실기 확인만 남음 `[대기: 실기기]` — 프로필 이미지·취향 사진이 같은 `<input type=file>` 경로(둘 다 `accept="image/jpeg,image/png,image/webp"`, 취향은 `multiple`)라 하나로 묶음. 취향 사진이 막히면 온보딩 핵심 플로우가 앱에서 끊긴다. **원안(image-picker + 브리지 + data: URL 전송)은 폐기** — 웹 업로드 UI 를 네이티브에 이중 구현하는 비용인데 react-native-webview 가 Android `onShowFileChooser`·iOS WKWebView 파일 입력을 이미 처리한다. 네이티브 설정만 채웠고([manifest](../../apps/mobile/android/app/src/main/AndroidManifest.xml)·[Info.plist](../../apps/mobile/ios/TriPick/Info.plist)) 브리지 코드는 넣지 않았다. **Android 는 권한 선언이 오히려 해가 된다** — 갤러리 선택은 SAF(`ACTION_GET_CONTENT`)라 `READ_MEDIA_IMAGES` 없이 동작하고, `CAMERA` 를 선언하면 `RNCWebViewModuleImpl.needsCameraPermission()` 이 "선언됐는데 미승인" 을 감지해 촬영 항목을 시트에서 빼버린다(미선언 시 시스템 카메라 앱 위임으로 그냥 됨). 대신 Android 11+ 패키지 가시성 때문에 `IMAGE_CAPTURE` `<queries>` 만 추가. iOS 는 사용 설명 문구가 없으면 접근 순간 크래시라 `NSCameraUsageDescription`·`NSPhotoLibraryUsageDescription` 추가. 실기 확인 항목 — ① 갤러리 선택 ② 촬영 항목 노출·촬영 ③ 취향 사진 다중 선택 ④ webp accept 동작. 하나라도 안 되면 그때 image-picker 폴백

## 테스트 · 운영

- [x] realtime 게이트웨이 인증/인가 e2e `[코드확인]` — 실 socket.io 서버를 임의 포트로 띄우고 진짜 클라이언트로 붙어 게이트웨이 인증/인가 왕복을 검증([realtime-gateway.e2e-spec.ts](../../apps/api/test/realtime/realtime-gateway.e2e-spec.ts)). JWT 는 실제 `JwtModule` 로 서명/검증, 멤버십(`canAccessTrip`)만 목킹해 DB 없이 분기 제어. 6케이스 — 무토큰/무효토큰 즉시 절단·유효토큰 유지+인박스 room 자동합류·`join-trip` 멤버 joined/비멤버 join-denied(+room 방송 미도달)·`evictFromTrip` 회수 시 `trip-access-revoked` 통지+room 방송 절단. 기존 e2e(happy path 1케이스)가 못 덮던 부정경로·인가 회수를 채움
- [x] preferences 서비스 CRUD 커버리지 `[코드확인]` — 기존 spec 이 `upsert` 기상/취침 검증만 덮던 걸 전 메서드로 확장([preferences.service.spec.ts](../../apps/api/test/preferences/preferences.service.spec.ts), 3→12케이스). `findByUser`(조회·null), `getPreferenceVector`(위임), `setPhotoUrls`(행 없으면 기본값 생성·재임베딩 스킵/사진 삭제 시 photoTags·disabledPhotoTags 정리), `upsert`(신규 생성·배열 dedup/부분 dto 병합/취향 신호 유무에 따른 임베딩 호출·embeddingId 반영) 커버
- [x] cron 을 바꾸면 옛 반복 스케줄이 남아 스캔이 두 벌 돈다 — 네 스케줄 모듈이 `queue.add(name, {}, { repeat })` 로 등록했는데, 이 방식의 repeat 키는 `hash(name:jobId:endDate:tz:pattern)` 이라 **패턴이 키에 들어간다**. cron 을 고치면 새 항목이 생기고 옛 항목이 남아 그대로 계속 발화한다(주석은 반대로 "같은 key 의 스케줄을 갱신한다" 라고 적혀 있었다). 실측 — 패턴만 바꿔 재등록하니 repeatable 2개·delayed 2개, `upsertJobScheduler` 로는 1개. **가설이 아니라 로컬 Redis 에 이미 있던 상태였다**: `scan-crowd`·`scan-weather` 가 같은 패턴으로 두 벌씩(키만 다름 — tz 를 나중에 붙이면서 갈린 것) 등록돼 있었다. 알림이 두 번 가진 않지만(weather·crowd 는 SET NX, arrival 은 `claimAlert` 로 발송권 선점) **외부 API 호출이 두 배**이고, 특히 crowd 는 KTO 일 예산 900콜을 두 벌이 나눠 쓴다. 키가 스케줄러 ID 하나뿐인 `upsertJobScheduler` 로 바꾸고([repeat-schedule.ts](../../apps/api/src/common/repeat-schedule.ts)), **같은 이름의 옛 해시 키는 등록 시 함께 지운다** — 안 지우면 배포 후에도 두 벌이 계속 돈다. 정리는 부수 작업이라 실패해도 등록을 실패로 만들지 않는다(다음 기동에 재시도). 실기동 검증: 잔재 7건 제거 후 큐마다 항목 1개(`key === name`)·delayed 1개, 재기동 시 제거 0건 멱등. 회귀 9건(673 tests)
- [x] `enableShutdownHooks()` 미호출로 배포마다 진행 중 잡 중복 실행 — @nestjs/bullmq 는 워커를 `onApplicationShutdown` 에서 닫는데 훅이 꺼져 있어 SIGTERM 에 그냥 죽었다. 진행 중이던 잡은 락(30s) 만료 후 stalled 로 잡혀 **처음부터 다시** 돈다 — 재계획이면 LLM 재생성·일정 재작성·완료 알림이 재배포마다 한 번 더. [main.ts](../../apps/api/src/main.ts) 한 줄로 해소했고, 덤으로 네 알림 모듈의 `onModuleDestroy`(재시도 타이머 정리)도 그제서야 실제로 동작한다. 훅이 매달려 종료를 막지 않는지 SIGTERM 실측으로 확인
- [x] replan 큐만 등록 타임아웃·보관 정책이 없다 — 다른 큐엔 다 있는 두 가드가 여기만 비어 있었다. ① `queue.add` 가 맨 `await` 이라 Redis 무응답 시 ioredis 오프라인 큐에 버퍼링돼 **`POST /replanning` 이 영영 매달린다**(사용자는 스피너만 본다) → `withTimeout`(10s) + 503 으로 [preference-analysis.enqueue](../../apps/api/src/preference-analyzer/preference-analysis.service.ts) 와 같은 모양. 승인 경로(`ScheduleChangeService.approve`)는 이 예외를 이미 잡아 제안을 failed 로 돌리고 요청자에게 알린다. ② `removeOnComplete`/`removeOnFail` 미지정이라 완료·실패 잡이 Redis 에 무한 적재 → `{age: 3600, count: 100}` / `{age: 86400}`. 잡 조회 API 가 없고 결과는 WS·인박스로 가므로 보관은 사후 확인용이며, 10초 버킷 jobId 와도 안 부딪힌다(재계획이 자기 버킷 안에 끝날 수 없다). 회귀 3건(676 tests)
- [x] main-planner swap/reorder/alternatives 커버리지 `[코드확인]` — 기존 main-planner 테스트가 addItem·createTrip·DTO 검증만 덮던 세 서비스 경로의 실동작 추가([main-planner.swap-reorder-alternatives.spec.ts](../../apps/api/test/main-planner/main-planner.swap-reorder-alternatives.spec.ts), 8케이스). `swap`(장소·좌표·카테고리 교체+이전 장소 보관·inbox 알림/앞뒤 이동시간 빠듯 시 경고), `reorderItems`(order 1..n 재배정+시각 오름차순 슬롯 재배정/개수·구성 불일치 400), `getAlternatives`(실후보 3+ realtime=true·폴백 미보충/무결과+note없음 mock 3개 보충/note 있으면 결과없음 노출/기 담긴 장소 dedup)

---

## 경계선 — 진행 여부 판단 필요

- [ ] 친구 등록 시 카카오 친구 API 연동 — §6은 카카오 **OAuth만** 문서화. 친구 목록 API는 스코프 확장 성격
- [ ] App Check 활성화(Play Integrity / DeviceCheck) `[보류: 운영 하드닝, 로드맵 밖]` — 제품 기획보다 운영 보안 하드닝
- [ ] hot-link 방지 / Object lock — 동상

## 내재적 한계 (액션 아님, 문서화만)

- iOS 앱 완전종료 시 위치 끊김 · force-stop/OS 강제종료 시 보고 불가
- 도보 직선거리 추정(강·산·고가 우회 모름, 1.3배 평균 계수)
- 중기예보 `regId` 도 단위 해상도(대표도시 nearest-centroid)
- 강수확률 폴백 경로(PTY·POP 항상 동반이라 사실상 도달 불가)
