{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"],
      "env": ["SENTRY_ORG", "SENTRY_PROJECT", "TRIPICK_API_ORIGIN", "TRIPICK_STORAGE_ORIGIN", "TRIPICK_PRIVATE_STORAGE_ORIGIN"],
      "passThroughEnv": ["SENTRY_AUTH_TOKEN"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env.test*"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    }
  }
}
